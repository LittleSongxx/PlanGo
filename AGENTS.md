# PlanGo 项目约定（当前仓库名 PlanGo）

已确认的项目决策见 [docs/架构决策.md](docs/架构决策.md)。开始设计或改动前先读该文件；其中的目标架构不代表已经实现。

接续实施时先读 [docs/CODEX_HANDOFF.md](docs/CODEX_HANDOFF.md) 顶部最新补充，再看历史基线和约束。本轮已授权广泛清理语义规则并接入统一任务负责人；当前工作树尚在验证，未部署，不把旧TSR结果套用新架构。旧六题两轮回归仍仅1/6通过；冻结后的独立新资料不得提前读取调优，旧集只称回归。真实商家完整准备/履约和独立使用验收仍未完成；[旧实施交接](docs/CODEX_实施交接.md)仅保留历史，不重复迁移/登录，不把受控测试当真实商家履约。

- 项目独立运行，不依赖兄弟 Planora 仓库、环境配置或服务；上游仅通过本仓库固定源码与显式维护操作参考。
- 用户于 2026-09-09 确认全面更名为 **PlanGo**；产品展示名使用 `PlanGo`，技术标识统一为 `plango` / `PLANGO_`。UI、源码包、Docker、conda、配置、脚本和文档都在迁移范围，不能只改展示文案。具体迁移与验收见交接文档 R0。
- 后端 Python 已迁入本项目独立 `plango` conda 环境，全部启动入口使用该环境。不得重命名、删除或改坏可能被兄弟项目使用的原 `planora` 环境。
- 保留 Electron 桌面与真实可见浏览器。浏览器演进方向已确认：WebContentsView、经适配验证的 Playwright/CDP、DOM-first 与按需 Vision，共用同一会话及 Harness 授权/回执边界。
- 不用 mock 补齐正常运行的商家事实或业务结果。测试样本与真实业务验收必须明确区分。
- **长期开发原则（2026-09-10用户再次明确）：功能实现和任务完成优先，全面避免Agent过拟合。** 失败时先沿完整调用链诊断意图理解/拆分、规划与协作、上下文及状态传递、过多规则造成的保守拒答；不能仅针对单个badcase叠加问法、实体、字段特例或指定任务规则。把局部补丁搬进共享函数不算根因修复。发现历史过拟合机制可直接清理、合并或重设计，优先让模型理解与交付完整任务、确定性工具完成计算和状态操作；未知只限制依赖它的结论。能力限制降低优先级，不新增无依据拒答门槛；支付、外部提交、删除、秘密访问等严重安全行为保留并最终统一核查权限、审批、幂等及UNKNOWN不重放边界。用同条件回归和实现冻结后的独立新任务验证泛化；工程测试数量或少答所得高Groundedness不能代替任务成功。
- 本项目 `.env` 中的密钥不输出、不提交；不要修改兄弟 Planora 正在进行的工作。
- 调试或验收打开的临时应用窗口暂不使用时及时关闭，避免用户误以为需要操作；需要用户扫码、验证码或审批时明确说明。关闭窗口保留 profile 和原任务，不把此约定实现为产品空闲自动关闭。
