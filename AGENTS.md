# PlanGo 项目约定

本文件只写**长期有效的约定**。它会被自动注入每个 AI 会话，所以不放进度、评测分数、"本轮"状态或阶段结论——那些放提交信息和 `eval/` 下的证据目录。看到这里出现状态描述，就是该删掉它。

## 长期开发原则（用户明确要求，优先于其他一切）

**功能实现和任务完成优先，全面避免 Agent 过拟合。**

失败时先沿完整调用链诊断：意图理解与拆分、规划与协作、上下文与状态传递、以及过多规则造成的保守拒答。不能针对单个 badcase 叠加问法、实体、字段特例或指定任务规则；把局部补丁搬进共享函数不算根因修复。发现历史过拟合机制可以直接清理、合并或重设计。

让模型负责语义理解与交付，确定性代码负责类型、计算、持久状态和严重外部动作边界。**未知只限制依赖它的那一条结论**，不阻断其余可交付部分，也不能被当作已确认。不新增无依据的拒答门槛。

严重安全行为——支付、外部提交、删除、秘密访问——保留权限、审批、幂等和 UNKNOWN 不重放边界。返回"未确认"意味着不声称成功，这类守卫不属于要清理的保守规则。

用同条件回归和**实现冻结后**由新上下文独立编写的新任务验证泛化。工程测试数量、或少答换来的高 Groundedness，都不能代替任务成功。

## 技术边界

- 项目独立运行，不依赖兄弟 `../Planora` 仓库、环境或服务；不要修改它。
- 产品展示名 `PlanGo`，技术标识 `plango` / `PLANGO_`。
- 后端 Python 使用本项目独立的 `plango` conda 环境。不得重命名或改坏可能被兄弟项目使用的 `planora` 环境。
- 保留 Electron 桌面与真实可见浏览器：WebContentsView、经适配验证的 Playwright/CDP、DOM-first 与按需 Vision，共用同一会话及 Harness 授权/回执边界。
- 集中式 LangGraph 工作流：行程层 Plan-and-Execute，浏览器层有界 ReAct。`single`/`multi` 是视角策略，不是两套运行时，也不代表已证明多角色收益。
- 不用 mock 补齐商家事实或业务结果。受控测试样本与真实业务验收必须分开表述。
- `.env` 中的密钥不输出、不提交。

## 协作约定

- 调试或验收打开的临时窗口不用时及时关闭，避免用户误以为需要操作。需要扫码、验证码或审批时明确说明。关闭窗口保留 profile 和原任务；不要把这条实现成产品的空闲自动退出。
- 已确认的架构决策见 [docs/架构决策.md](docs/架构决策.md)。其中记录的是决策，不代表已经实现。
- 评测方法见 [docs/质量评测协议.md](docs/质量评测协议.md)；每批的实际结果、判据与审核记录在 `eval/<批次>/` 下，以那里的原件为准。
- 想知道"现在做到哪了"，读 `git log` 和 `eval/` 下最新批次的 README，不要依赖任何文档里的状态叙述。
