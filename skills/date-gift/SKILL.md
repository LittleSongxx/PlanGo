---
name: 约会送花
description: 约会场景：按双方偏好选择氛围餐厅与从容动线；有送花或蛋糕需求时核对费用、接收政策与送达时点。
operations:
  - snapshot
  - extract
  - navigate
  - scroll
  - click
  - type
  - finish
---

# 约会送花程序

有界程序：不授权新工具。已加载后只能使用 frontmatter 列出的浏览器操作。`read_skill` 只用于加载或切换程序，不是本程序的业务步骤。准备态禁提交由执行层强制。

1. 观测：对当前页 `snapshot` 或 `extract`；政策或菜单未读完时 `scroll` 后再观测。
2. 可选导航：仅当用户已给出 http(s) URL 时 `navigate`；不要编造地址。
3. 核对：用当前快照核餐厅氛围描述、鲜花/蛋糕接收政策、费用与配送时段。没有可用页面就列出待确认事项；评分与排队未知时标明。
4. 需审批的写入：每个 `click`/`type` 单独提出，只使用当前快照 idx。
5. 未知或受阻：登录、验证码或能力不足时暂停并交给用户。读回页面后仍需核业务要求；未核实的下单或配送保持未知，不补造回执。
