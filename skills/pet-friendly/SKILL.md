---
name: 宠物友好
description: 带宠物出行场景：优先可携宠的餐厅/公园/露营地/宠物友好商场，避开明确谢绝宠物的场所。
operations:
  - snapshot
  - extract
  - navigate
  - scroll
  - click
  - type
  - finish
---

# 宠物友好程序

有界程序：不授权新工具。已加载后只能使用 frontmatter 列出的浏览器操作。`read_skill` 只用于加载或切换程序，不是本程序的业务步骤。准备态禁提交由执行层强制。

1. 观测：对当前页 `snapshot` 或 `extract`；政策未读完时 `scroll` 后再观测。
2. 可选导航：仅当用户已给出 http(s) URL 时 `navigate`；不要编造地址。
3. 核对：用当前快照核可携宠政策原文。仅有户外位不能证明可携宠；未写明的限制与预约情况标未知。
4. 需审批的写入：每个 `click`/`type` 单独提出，只使用当前快照 idx。
5. 未知或受阻：登录、验证码或能力不足时暂停并交给用户；不要宣称预约或履约已经完成。到店政策以页面与用户确认为准。
