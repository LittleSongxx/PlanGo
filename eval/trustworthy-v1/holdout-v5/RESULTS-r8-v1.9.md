# holdout-v5 第 8 轮与评分器 v1.9：观测面与原子子句

同一数据集（金标 accepted）、产品/评分器组合不同。动机：r7b 的 F=0.51 归因发现
三个**评分器**缺陷（非产品幻觉）——预设值无观测来源、整句聚合连坐、声明语境半句裸露。
TSR 与 F 对同一读卡交付给出相反裁决（field_equals 全过 vs F 全零）是最强证据。

| 报告 | 产品 | 评分器 | TSR | F（llm） | 计分 |
| --- | --- | --- | --- | --- | --- |
| r7（对照） | `89adacb` | v1.8 | 0.951 | 0.517 | 99 |
| **r7c** | `89adacb`（同一份 attempts 重评） | **v1.9** | **0.951** | **0.846**（下界 0.812） | 161 |
| r8 | `5db7127`（+算式附注修复） | v1.9 | 0.931 | 0.861 | 163 |

## 评分器 v1.9（`SCORER_V1_9_NOTES.md` 全文）

1. `with_contract_values`：任务 `initial_trip_spec` 并入观测（"任务给定合同"）；
   **不读**本跑 spec——产品写错的值不能给自己作证。
2. claim 拆分到逗号子句（原子粒度，与 RAGAS/FActScore 一致）：一个无来源字段
   不再连坐整句。
3. 声明句的无数字子句继承 structure 豁免；有数字子句照旧独立进分母。
4. `with_user_turns` 标签更名"用户全部输入"（内容本就是全部轮）。

r7→r7c 隔离验证：同一份 attempts 只换评分器，F 0.517→0.846，TSR 逐字节不变
（0.951，程序化判定不受 F 改动影响）。覆盖 99→161（boundary 19→30、
conflict 1→34、unknown 12→29）。persist 层 34×0.000 的误判消除。

## 产品修复（`5db7127`）与 r8

`_calculation_trail` 此前把减法也印成加号（"794+718=76"——对用户是自相矛盾的
算式），变量名 operand 原样印出（"songfengzao_total+…=567"）。修复后各运算印
自己的符号（× − ÷ +）、operand 用计算器实际解析的数值。

r8（190/204）相对 r7 的 9 挂 / 5 过翻转中：2 题（calc-14/24）是附注数字化后
substance 落到 29/28 的**阈值边缘交互**（1770/694 为中间计算结果，页文本就无
标签可带——修复行为正确）；conflict 2 题（声明缺 records）、persist 3 题
（提取噪声）、boundary +3（拒答复述改善）属方差区。**正确性优先**：算式自相
矛盾必须修，1–2 字符的边缘判例不做特例修补（AGENTS.md），记入金标准裁点②的
已知判例。

## 对外引用口径

**TSR 0.951（Wilson 95% [0.912, 0.973]）/ F 0.846（bootstrap 95% 下界 0.812）**
——出自 r7c（`holdout-v5-report-r7c-llm.json`，金标 accepted 数据集 + 评分器
v1.9 + 产品 `89adacb`，单一报告内自洽）。r8 为后续正确性修复的记录
（F 0.861、TSR 0.931，含上述边缘交互与方差）。全部 provisional_holdout。
