# holdout-v3 复测结果（r6 方差复跑 + r7 评分器 v1.5）

- 金标状态：**accepted**（2026-09-12 独立会话全量审阅，见 `gold_review.json`；时间锚倒置的 major
  遗留见该文件与 `README.md`）。`evaluation_kind=holdout_reviewed`，`report_kind` 仍为
  `provisional_holdout`（正式报分前须修时间锚并复核 runner 审计）。
- 本套仍是实现流生成的受控开发集，不支撑「未见泛化」结论。

## r6：同码复跑（方差测量）

与 r5 完全同码：`product_sha=32e8b5337387`（r5 记 `65a54a4` dirty、r6 记 `c44abf9` clean，
产品文件一致）。评分器 v1.4。

| 指标 | r5 | r6 |
| --- | --- | --- |
| TSR | 0.926（189/204） | 0.931（190/204） |
| F（宏平均） | 1.000 | 0.990 |
| F 计分题 | 102 | 103 |
| 只回不确定词 | 68 | 67 |

**逐层只有一处差异**：`h3-calc-016`（词表族）r5 失败、r6 通过——正是 RESULTS-r5 点名的
回摆形态。calculate novel 两跑都是 7/20，conflict/persist/sparse 全同，unknown 均 33/34。
同码方差 ≈ **±1 题**；此前「单层 ±3 题」的保守提醒可以收窄为「±1–2 题」。

## r7：评分器/产品 v1.5（commit 917f811，干净快照）

v1.5 = 缺口结构化声明（`uncertainty{kind,subject,records}`，见 `SCORER_V1_5_NOTES.md`）。

| 指标 | r5/r6（v1.4） | r7（v1.5） |
| --- | --- | --- |
| TSR | 0.926 / 0.931 | 0.926（189/204） |
| F | 1.000 / 0.990 | 0.990 |
| 层差异 | — | conflict 34/34、unknown 34/34（历史最高）、persist 33/34（-1，方差内） |

三项验证结论：

1. **旧产物不受影响**：r5 attempts 在 v1.4/v1.5 规则判分下逐题同分（TSR/F 完全一致）。
2. **结构化路径在本套上是保险而非增量**：模型填写 `uncertainty` 的只有 12/68
   （conflict 7、unknown 5），且这 12 题的答复句均已被 v1.4 词表判 non-factual，
   `by=structure` 路径没有触发——即当前问法下词表够用，v1.5 的价值在**抵御下一批新说法**
   （r3「资料缺该值」那类翻车不再可能）与提供「结构采纳率」这个可测信号。
3. **采纳率低是真实缺口**：schema 描述只带来 ~18% 填写率。下一步若要让它可考核，
   应在下一版数据集给 unknown/conflict 层加「结构已声明」检查（TSR 六型之外新增一型，
   属合同变更需评审），而不是在产品侧加硬拒绝（拒绝烧整轮，r5 的 33/34 会被拖垮）。

## v2 四份交付的 v1.4 重评（口径补齐）

| 交付 | v1.3 F | v1.4 F | TSR（不变） |
| --- | --- | --- | --- |
| v2 首跑 | 0.770 | 0.858 | 0.985 |
| r2 | 0.887 | 0.884 | 0.966 |
| r2b（12 题） | 1.000 | 1.000 | 1.000 |
| r3 | 1.000 | 1.000 | 1.000 |

TSR 逐批与 v1.3 完全一致，印证 v1.4「只动分母、不放松判定」。

## 当前未解（与 r5 记录一致，无恶化）

- calculate 14 题失败（novel 13 + shared 0–1 摆动）：用户可见数字全对、未走计算器。
  裁决选项 A（本套 oracle 勘误）/ B（下一套改题面）见 `PROPOSAL_STRUCTURED_GAP_v1.5.md` 附注。
- F 计分覆盖 0.50：sparse 空交付（设计使然）+ unknown/conflict 整段非事实。
- 金标 major：`as_of`（09-20）早于全部 `observed_at`（09-21），正式报分前须对齐并留痕。
