# 真实门店读取与界面优化（2026-09-09）

用户在独立 PlanGo 窗口完成大众点评扫码登录，并在重开后手动完成美团验证中心要求的安全验证。未自动识别/操作验证码，未复制 Cookie。沿用 `output/merchant-next/session-C0ovDv/` 和原任务 `f8f8ad2719074b0a9c62cbb08367892f`，保留此前全部失败与累计用量。

## 实际取得的内容

[读取记录](read-result.json)来自真实[门店页面](https://www.dianping.com/shop/HaQlfCl5X2CeJdSs)，不是商家fixture：顺风123(观音桥大融城店)，地址为观音桥步行街8号附5号大融城6楼6-010，页面人均参考62元。50元代金券售价47元，精选双人餐98元，顺风冷面鸡单品优惠19.9元；只保留10条能单独辨认的推荐菜，末尾连续长串不擅自拆分。

金额是记录时间的页面显示，不能当实时库存/成交价。面值不当售价；划线数字没有提升为已核验原价，`original_price=null`。周一至周日、随时退、过期自动退是页面预览标签，仍缺完整使用规则。菜单详情要求使用平台App，当前页面无原生预约表单。

结果明确保持 `PARTIAL_FAILED/needs_evidence`、`scope=read_only`、`business_completed=false`。已读门店身份/地址/推荐菜/优惠条目，完整菜单及使用细则未覆盖。全历史11条浏览器命令均为extract，动作账本0；没有预约、下单、支付或外部消息。

## 实际界面与修复

- [最终界面](live-result-final.png)、[恢复核对](ui-restored.json)：1800×1120默认窗口，屏幕较小时限制到工作区；地址/人均/价格优先，长标题、原文与quote折叠，未知单价不做无依据比价。推荐菜默认8项、其余可键盘展开；反馈位于结果之后。
- [旧观测覆盖新卡片的失败截图](live-result-before-order-fix.png)保留。恢复补投影可能在新结构化结果后追加旧raw command；界面现在按观测时间和当前snapshot消歧，而不是把数组末项当最新。修后重开只展示原任务已保存资料，未新增模型用量。
- 短页面保留全文，避免Readability裁掉店名/地址/优惠；首站适配仅匹配精确域名、shop路径、标题和顶部店名一致的实见版式，所有quote是同实体的连续原文。支持独立货币行的售价，错误店、面值、原价行、跨券串价、推荐商家、非有限数值均有反例。适配与通用抽取共用原有持久处理记录，旧processed结果不改。
- 复合读取按明确字段逐项核对；“当前浏览器页面”等表达只用同URL/同snapshot证据。推荐菜/条件预览不填平菜单/完整规则，历史未绑定摘录用中性标签。

临时窗口不使用时已关闭，SQLite后端也已停止；完整profile、身份、登录态和数据库保留。不能用本验证的Cookie存续证明跨机器密钥环迁移。

## 用量与验证边界

累计12152 tokens、5 tools；最后一轮baseline8809/3，增量3343 tokens/2 tools。真实记录是一次模型请求及后续`model_token_budget` fallback，不是0模型成功。预算baseline正确，fallback来自字符/schema/system/skills保守估算及收尾预留；没有改额度或清累计。后续对只有App下载链接的明确菜单预览跳过无效模型循环已通过受控回归，尚未伪造新的0模型真实对照。

`output/merchant-live/`保留所有中间失败、私有完整snapshot与日志；本目录只提交脱敏来源、结果及界面证据。完整质量指标测评继续暂停。真实预约准备还需要实际可操作网页入口；此页面只交付上述读取范围，不能用本地表单补成真实商家履约。
