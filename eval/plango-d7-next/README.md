# D7 真实入口与独立试用准备

2026-09-09，从`e8e156b`继续。此目录区分真实网站只读调查、独立试用交付和待授权的准备操作；没有完成商家预约或独立用户验收。

## 已找到真实入口

候选是**重庆尼依格罗酒店·悦廊 The Tea Lounge**，是单独调查的候选，没有替换原顺风123行程。

[餐厅官网](https://www.niccolohotels.com/en/niccolo-chongqing/dining/the-tea-lounge)的 Book A Table 链接到[官方订位供应商入口](https://www.szuo.com/en/shops/niccolo-chongqing-tealounge/reserve)。检索限4查询/5页面读取；检索工具访问订位页超时，但本项目独立Electron实际打开成功。官网在当前浏览器地区跳转至`.cn/en/home`的行为也保留，不隐去这次差异。

预约页先展示商家须知，点击实际“Confirm and continue”导航按钮后，出现人数、日期、时间和 Find availability 控件。第一次按role定位同时命中wrapper和真实button而未执行；核对DOM后精确定位实际button。网站重开会回到须知页，第二次仅重复查看控件。

[真实网页截图](real-booking-controls.png)和[字段及来源摘要](real-entry.json)证明页面已找到。截图中的2人/9月9日是网站默认值，没有选定时间；不代表用户要求、空位或预约成功。须知含2025年旧优惠限制，不能作为当前优惠可用结论。

没有改人数/日期/时间、没有查询空位、没有输入个人信息或提交预约；没有新建业务run或调用模型。独立profile留在`output/d7-next/tealounge-brha972n/`，原始现场在`output/playwright/d7-next/`；窗口/SQLite后端已关闭，原顺风123任务保持。

## 尚待准备适配与授权

实际页面使用自定义按钮控件，DOM原生form数量为0；`szuo.com`不在现有自动写入站点列表。已有Preparation要求完整同商家原生表单，所以现在不能宣称`ready_to_review`，也没有为凑结果改造或伪造form事实。

已向用户提出具体候选及建议验证条件：2人、2026-09-11 15:00，仅逐项准备/查询空位、不提交。**尚未得到该范围的确认**；后续以用户实际回复为准，再沿已有审批、快照、回执和版本做最小适配。网站重开回到须知的情况也需要纳入恢复验收，不能假定字段已经保留。

## 独立试用交付

最新包及SHA256以[包记录](package.json)为准；功能基线为`e8e156b`，另包含本轮安装说明和[独立试用记录](../../docs/独立试用验收.md)。保留首个`product-next`归档及其[记录](package-e8e156b.json)，最新使用`d7-ready`文件，旧包不覆盖。

复用既有打包脚本，只把验收说明加入明确允许的包内文档清单；构建、包内文件/密钥排除/作者与固定上游保留检查，以及既有trial受控回归通过。没有启动另一套Docker、重做安装/升级/冷恢复测试或发布归档。试用者尚未实际完成记录，不把开发检查算用户采用；未代发邀请。

## D5范围

本次真实浏览仅做[两次被动资源采样](resource-observations.json)。进程数和加载站点不同，RSS相加可能重复计算共享页，不能从前后增长推断泄漏；也不能据此证明长期稳定或解释WSL崩溃。继续串行、单个临时窗口，未恢复质量sweep或运行长时压力任务。
