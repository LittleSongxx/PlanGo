name: PlanGo checks
on: [push, pull_request]
permissions:
  contents: read
jobs:
  integration:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - uses: conda-incubator/setup-miniconda@v4
        with:
          miniforge-version: '26.5.3-0'
          activate-environment: plango
          python-version: '3.12'
      - run: npm ci
      - run: npm exec -- install-electron --no
      - run: npm run setup:backend -- --dev
      - run: npm run check
      - run: python scripts/check_lifecycle.py
      - run: python scripts/migrate_config.py
      - run: env -u ELECTRON_RUN_AS_NODE xvfb-run -a npm run test:desktop-storage-browser
      - run: python -m ruff check backend/plango backend/tests
      - run: MYPYPATH=backend:vendor/plango_harness/backend python -m mypy backend/plango vendor/plango_harness/backend/plango_harness
      - run: python backend/plango/migrations/check.py
      - run: npm run test:independent
      - run: env -u ELECTRON_RUN_AS_NODE xvfb-run -a npm run test:browser
      - run: env -u ELECTRON_RUN_AS_NODE xvfb-run -a npm run test:browser-view
      - run: env -u ELECTRON_RUN_AS_NODE xvfb-run -a npm run test:desktop
      - run: env -u ELECTRON_RUN_AS_NODE xvfb-run -a npm run test:map-title
      - run: env -u ELECTRON_RUN_AS_NODE xvfb-run -a npm run test:full-stack
      - run: python scripts/check_service_recovery.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: service-recovery-check
          path: output/service-recovery-*/report.json
          if-no-files-found: ignore
