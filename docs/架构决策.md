# PlanGo 已确认架构决策

首次记录：2026-09-08；更新：2026-09-09。此文保存用户已经明确认可的方向；替换方案的后续建议应与这些决策区分。PlanGo 全面更名及数据升级正在按 R0 收尾，当前证据与后续阶段见 [实施进度](实施进度.md)。

## ADR-001：独立的真实浏览器 Agent 项目

状态：已确认；独立源码、配置、Docker 与 conda 运行路径已有实现和验收证据。

PlanGo（原 YOYU）保留现有产品能力与真实浏览器操作，吸收固定上游源码中的有用 Harness 机制，自行验证和改进质量。日常构建/运行不读取兄弟 Planora 仓库或服务，也不随其正在进行的测评修改自动漂移。原实现使用 `planora` conda 环境；新目标为独立 `plango` 环境，按 ADR-003 迁移。

## ADR-002：保留 Electron，演进浏览器承载与执行层

状态：WebContentsView、经实测的Playwright/CDP与只读Vision已落地；坐标动作尚未实现，真实商家能力仍须按任务验证。范围与失败见 [实施进度](实施进度.md) 和 [浏览器适配验证](浏览器适配验证.md)。

1. 保留 Electron 与 React 桌面工作台。Electron 是适合本项目的成熟桌面方案；具体 webview API 的风险不代表需要替换 Electron。
2. 浏览器承载层逐步采用 WebContentsView，处理窗口尺寸、焦点、视图切换和生命周期。
3. 优先采用适配验证通过的 Playwright 能力；Electron 特有控制缺口再用受控 CDP 补足。Playwright/CDP 是执行层，不能替代上层业务契约。
4. 用户看到的页面、模型观察的页面、执行器操作的页面必须属于同一个受控浏览上下文，保留真实登录会话与人工接管。
5. 采用 DOM-first：语义定位、正确 frame、开放 Shadow DOM、有界等待优先；视觉按需用于理解、消歧、无法结构化表示的目标定位或动作后核验。
6. 视觉目标能映射回 DOM 时优先用 DOM 动作；否则需校验截图时间、页面、裁剪、缩放及坐标映射后再操作。
7. DOM 和 Vision 使用同一命令、授权、幂等、回执及后验规则。拒绝授权、旧审批失效、登录/验证码接管和提交结果 UNKNOWN 不得通过切换视觉绕过或重放。
8. 原始任意脚本/调试协议权限不直接交给模型；固定操作与授权边界应由受信执行层持有。

采用顺序：浏览器会话接管验证 → 成熟 DOM 执行与连续操作 → 按需视觉能力 → 相同任务集下 DOM-only/hybrid 的质量、误操作、接管、延迟与用量对照。

依据与约束见 [闭环与设计成熟度评审](闭环与设计成熟度评审.md)。后续项目全量替换审计中的库选型、状态/记忆/评测方案仍是待评估建议，不因出现在报告中就成为已确认决策。

## ADR-003：全面更名为 PlanGo

状态：用户于 2026-09-09 明确要求；源码、环境和部署已实施更名；以实施进度中的本轮验收和遗留项为准。

- 产品展示统一为 `PlanGo`；技术标识统一为 `plango`，自有环境变量前缀为 `PLANGO_`。覆盖桌面 UI、窗口/应用标识、源码及导入、包元数据与锁、Docker 项目/容器/镜像、conda、配置、启动/停止脚本、测试、文档和项目目录。
- 自有 Python 主包目标为 `plango`；吸收的 Harness 运行时包也应使用 PlanGo 命名（如 `plango_harness`），检查导入、迁移、序列化类型和工具路径后迁移。第三方版权、许可证、上游来源与固定提交信息保持真实，不能将来源事实改写为原创。
- 创建独立 conda `plango` 并按本项目依赖安装验证。保留原 `planora` 环境；不修改兄弟 `../Planora` 仓库或服务。
- 更改 Docker、数据库/队列命名和 Electron 数据目录前，确认归属、备份并制定迁移与回滚步骤。保留会话、登录态、审批、幂等记录和用户数据；不能以启动一个空数据库冒充完成迁移。历史审批保持原绑定或明确失效后重新审批，不能通过改字符串授权或重放 UNKNOWN 提交。
- 当前工作目录是 `/home/song/code/Agent/multi-agent/YOYU`；目标目录为同级 `PlanGo`。目录迁移放在当前会话文件操作完成后进行，先检查目标冲突并更新路径引用。保留用户已有融合方案文件及历史证据原文。
- 执行顺序为交接文档 R0 更名与数据迁移验收，再按 P0→P3 完成原有正确性、浏览器和工程改进。将机械更名与行为修改拆成可审查增量，分别验证。
