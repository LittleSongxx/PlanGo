# PlanGo 实施进度

2026-09-09 接续；以新验证记录为准，历史评测不代表本轮通过。

**新对话入口：[CODEX_HANDOFF.md](CODEX_HANDOFF.md)。** R0→P3 本轮核心实施已收尾，当前验收见本文末尾“本轮收尾结果”。正文按时间保留的“正在修/待复验”等表述是阶段记录，不是最新待办。2026-09-09 08:10 只读交接复核：主 Docker 服务健康，主库有效租约/待命令为 0；当前未发现存活主桌面进程，不能以旧 PID/CDP 文件认定仍在运行。测试服务已停止、数据保留；后续建议见新 handoff 第 7 节。

## 最新目标（用户更新）

在保持正常功能的前提下持续优化主线、闭环和真实可用性，并全面改造为现代、美观、独立的 PlanGo UI。真实 API 调用仍获授权；本轮暂停完整质量指标测评，不进行角色收益、DOM/hybrid 等整批指标 sweep。功能回归、失败定位、数据连续性及有界真实场景验证继续执行。

架构要求：明确单/多模式、Plan-and-Execute、浏览器ReAct、协调器与专业节点，以及记忆、MCP、Skills的选型；实现混乱须修代码，不能靠文档重新包装。当前说明见 [Agent架构与选型](Agent架构与选型.md)。

UI方向：独立品牌与一致设计tokens，清晰工作区/任务/成果/浏览器关系；保留所有入口、人工接管、审批、地图、发现、分享、历史、记忆和设置，不用减少功能换取视觉简化。

## 基线与范围

- 工作区已于本轮迁至 `/home/song/code/Agent/multi-agent/PlanGo`（原同级 `YOYU`）；分支 `feat/planora-browser-harness`，起点 `6b52d6c`。
- 接手时用户修改：AGENTS、实施交接、架构决策；未跟踪接手提示词与融合方案。融合方案原文保留，SHA256 `4e63df2f9bf469b142ce6fc03b321f04da8337bf6d0a9ad817433197dca3a70d`。
- 初查本项目 Docker `yoyu` API/PostgreSQL/Redis 健康、worker 运行。原 conda `planora` 存在，`plango` 尚未建立；目标目录不存在。
- 私有备份 `output/r0-backup`，不提交，包含原配置与用户交接文档；禁止输出密钥。
- 不修改兄弟 Planora、原 conda 环境及其他项目容器。

## R0：已验收（本机迁移与隔离全新安装）

- 主控：归属/任务/存储盘点、备份、独立环境、启动/部署脚本、数据切换与文档。
- 桌面：`src` 与 JS/TS 检查脚本，包含 userData、partition、配置、localStorage 的无损迁移。
- 后端：主包 `plango`、内部包 `plango_harness`、新增 Alembic 表迁移、严格 checkpoint 类型兼容。
- 运行资源审计独立只读执行；切换之前停止旧消费者，备份后保留旧卷与回滚入口。
- 独立 conda `plango` 已按锁安装，解释器 `/home/song/miniconda3/envs/plango/bin/python`，Python 3.12.14；原环境未修改。
- 本项目无有效租约/待执行命令，原库4 tasks（3 SUCCEEDED、1 REQUIREMENTS_READY），18命令，49 Cookie；没有当前 UNKNOWN，但升级测试显式覆盖 UNKNOWN 与审批。
- 私有 PG逻辑导出、3卷冷备份、完整桌面profile已保存；新卷复制1393/6/1条文件元数据与内容逐项验证。旧4服务保持停止、旧卷保留。
- PG数据库与role已改名，原SCRAM凭据有效；16张业务/checkpoint表迁移前后摘要完全相同。真实682条checkpoint可读，32条旧模块路径严格转换恢复。
- Redis两个stream及消费组改名，22条run消息消费位置保留，PEL=0；独立Redis夹具验证非空PEL、重试、TTL、并发ack和冲突拒绝。
- 新Docker API/PG/Redis健康、worker已启动；部署API 23项通过。pytest49 +35 subtests、ruff、mypy64文件、迁移检查、独立复制启动与lifecycle检查通过。
- 桌面39条Chromium、离线UI及合成profile迁移通过；真实启动发现Crashpad自建目录冲突，修复中。full-stack首次失败因测试未初始化身份，保留失败并调整测试顺序。
- 实际桌面 profile 已完整迁到 `~/.config/plango`，49 Cookie关键字段与身份/回执原字节一致；Crashpad启动目录识别、真实主进程失败退出及界面ready标记已回归。
- 完整本地Python/Electron读取→重启→历史恢复通过，0模型token。真实部署模型+受控菜单页→API/worker重启→UI历史恢复通过，986 tokens；该测试单独归档，原16张表每一原始行仍完整存在。
- 实际重复start复用、重复stop保留卷均通过；进程title单参数形式已补准确归属判断。目录安全迁至PlanGo后重新启动与重复start通过，Compose归属标签来自新路径。
- 最终 `npm run check`：52 Python测试+35 subtests，全部TS检查和构建通过。SQLite冲突/中途故障事务回滚、旧默认目录及旧shell变量保护已补定向验证。
- 新证据见 `eval/plango-r0/`；上述测试不是真实商家履约。全新Docker隔离PG/Redis/migrate/API/worker安装已通过，测试卷已清理，未调用外部模型。

R0 本地提交：`4a5bf11`，CI补全 `4451aa4`；P0提交 `f845e9c`。随后全量跟踪文件审计发现 `.github/workflows/checks.yml` 尚有旧环境/路径，已补齐新命名并加入配置与真实profile迁移检查；远端 Actions 尚未运行，不能将本地通过当作远端 CI 证据。

## P0：核心修复已验证，跨阶段依赖继续跟进

- 桌面反例修前11项失败：小写连接错误、等待/运行/UNKNOWN、手填来源/伪精度、城市变更遗留坐标；正在修共享投影与Location契约。
- 真Chromium+实际PlanMap+AMap受控fixture，恶意标题修前执行测试sentinel并创建img；安全textContent修后sentinel不执行、无img、原文正常显示。该测试证明当前组件边界，不是外部站点攻击。
- 后端候选与Verifier已共享三态证据判断；无证据/过期/异店/否定或条件声明保持unknown，明确正反证据分别满足/违反。原始12项定向检查通过后，补完整声明语法及否定反例；最终全套76测试+35 subtests、ruff/mypy与TS/构建均通过。
- 普通链接将通过绑定href的原生导航继续，其他可能提交的click保留UNKNOWN；不宣称任意商家表单已全部自动完成。
- 真实Chromium47条、桌面来源/权限、地图标题与本地完整链路恢复通过。新的ExecutionGoal绑定plan/version/approval/需求与stops，过期证据或旧审批不能直接执行；图片“帮我规划”进入规划链。
- Preparation商家/人数/时间/入口正向后置验收仍缺，当前finish明确部分完成，P2补齐；不能把防止假成功当作业务闭环。
- 浏览器原始eval权限将在P1受信执行层迁移时收回。

## P1：主执行层与只读 Vision 已验收

- 隔离Electron33.4.11/Chromium130 + Playwright1.63探针：完整14/15；原生截图裁剪2/2；缩放交互及主进程端口发现5/5。
- 保留Playwright缩放截图失败；改用实测native capturePage校准。自定义popup→WCV会卡住，采用同session的可见BrowserWindow弹窗。未覆盖真实商家/OAuth/闭合Shadow，不能由fixture推断成功。
- 已迁入主进程WCV管理器+Playwright/CDP driver；renderer仅有固定用户意图和布局，原始JS/伪造回执IPC移除。保持同一会话，原生弹窗同步继承任务归属。
- 审查复现并修复：取消后的迟到主/子frame导航、refs截断之外/动态显现的登录门、隐藏期间的type、popup被其他run抢读、纯CSS/viewport及长文字改价、无关关闭target的发现竞态。回归包含拒绝与fresh snapshot正向路径。
- 主桥真实浏览器66条、视图生命周期40条；driver核心14/14+补充3/3与其余定向记录独立保存；中间失败不覆盖。最终npm check：108 pytest+35 subtests、TS/构建通过。
- 实际配置模型读图5/5字段、1请求1645tokens；本机显式启用每turn最多一次只读Vision，示例默认off。DOM部署菜单1044tokens并恢复通过。
- Canvas部署完整链首次已读对但重复vision建议触发错误partial（2683tokens），已修规则并保留失败；同场景复验2772tokens，1次截图、2观测、无click/type/业务receipt，API/worker重启与历史恢复通过。
- 当前Vision用于只读理解/核验及提供DOM决策参考；无坐标动作，不能宣称任意canvas操作或真实商家履约。Preparation的正向验收继续P2。新证据位于 `eval/plango-p1/`，适配边界见 `docs/浏览器适配验证.md`。

## P2：主线已实现，真实用户流程正在复验

- 规范需求稀疏补丁与差分刷新；显式工作流组合点替代父图删边，专业子图补齐Evidence/规范输入。单/多模式限定为视角策略，不改写其他专业角色提示词。
- 统一高德后端入口，附近使用坐标半径、城市参考使用同城搜索；缓存原时间、强制刷新选店详情、真实起点与搜索中心分离。普通规划不需预先打开标签。
- ExecutionGoal按来源验收；Preparation核对同一原生表单的门店、人数、日期、时间和提交入口，交付ready_to_review并明确没有提交。改人数后旧审批失效的完整API图路径已过。
- 新版UI已覆盖工作区、导航、成果、设置、发现、历史、连接、路线、分享、明确偏好与反馈，真实Electron夹具验证布局及WCV遮挡恢复；`eval/plango-ui/`为受控夹具证据。
- 真实模型+高德验收使用隔离`plango-e2e`数据库/Redis与独立桌面profile，主库不做测试清空。已通过明确地名定位、真实附近POI/图片、路线、选择POI创建任务、分享投票/留言回桌面与并入需求。
- 真实失败保留：模糊“重庆观音桥”返回另一区同名地点，设置补实际解析地址/粒度提示；选择重庆寿司郎后模型把门店名当起点并编码到香港，正在修canonical选店与真实起点边界；“步行街8号”触发错误距离澄清，规则已修待部署复验；步行约束仍查询驾车路线，正在接明确交通模式。不能把草案存在当整条规划路径已验收。
- 高德复杂营业时段、空路线、过期天气与错误日期已补定向校验；真实无排队/库存来源继续unknown。新增持久 `draft_review` 与 exact plan/version/interrupt 决策：保存草案交付 `draft_ready`；明确“仅准备表单”保留全部 `pending_checks`，只有当前可信门店/地址、日期/人数且没有已知硬冲突才可进入。普通提交按钮不因准备确认得到执行权限；9 项辅助回归覆盖保存、准备、编辑失效和阻断边界，真实 UI 重走进行中。
- 原真实任务等待期间的选店身份证据过期已定位；新增同一任务 Graph 入口按原 POI ID 真正刷新详情，每 turn 有界并计工具预算。成功才替换活动身份来源，失败不续旧证据；原用户起点、锁定计划、checkpoint/事件保留。7 项辅助回归包含同一暂停 run 重新核验、日期变更、失败/错 ID/预算边界；原真实任务 `d51586748f934797a6c8b202ccd8df5b` 部署复验待主控确认。
- 同一真实任务已取消预算后仍解释“远低于300元预算”，根因是当轮专业模型读取累积 goal 中的历史预算。Planner/Advocate/Critic 现只接当前规范字段，goal/messages 留作审计；当前报告按 run/turn 过滤并绑定分派角色，历史报告/trace 不删除。实际编译子图回归检查模型输入不含旧目标/旧报告，同时保留当前人数、时间、硬约束和偏好。
- 原准备 Goal v3 的真实表单时间18:30与计划18:31不一致，系统曾错误请求Vision并结束。已加入严格同form唯一字段差异→待审批type、原Goal显式继续入口，以及输入后新快照替换当前旧表单的规则。合成观测辅助API链验证PARTIAL→同Goal继续→新只读→逐项审批→持久输入回执/新快照→ready_to_review；原同run实际18:30→18:31修正由桌面继续验收，不预先改fixture值。

## P3：工程闭环实施中

- 主进程统一SSE订阅，Last-Event-ID续传，终态必须追平event_seq；删除renderer与App多层轮询。真实HTTP分块UTF8、断线重连、重复与尾事件回归通过，真实API断线/恢复待最终复验。
- 技能开关复用原配置跨重启保存；只影响新任务的技能快照。浏览器按用户读取明确偏好，可信只读/准备结果以 run/turn 幂等投影 episode；评分不推断偏好，UNKNOWN 不记成履约。反馈重复/冲突、episode 删除重试、过期过滤、清空后旧终态恢复不复活已覆盖辅助 API/图回归；真实 UI 反馈与遗忘路径继续验收。
- Embedding关闭时读取也不建立客户端；关闭状态不借用聊天密钥。定向opt-out检查通过。
- Browser结果CAS与BROWSER_OBSERVATION同事务落库；故障注入回滚、重试、重复与冲突通过，已保存截图的重复回执不因变旧而拒绝。
- Redis死信发布和ACK使用原子脚本：发布失败保持PEL，重复确认不重复死信。真实隔离Redis的错误类型故障/重试验证通过。
- 模型请求分类为鉴权、权限、配额、限流、超时、连接、服务/请求、结构化和内部错误；只有结构化回复错误做JSON repair。安全元数据与用户可读原因保留，网络失败不再伪装成本地成功。
- 本轮不开展完整质量指标测评。真实UI验收记录在`eval/plango-live-ui/`，外部网页/图片入口由独立隔离桌面验收，最终结果尚未全数收敛。


### 本轮集成与真实复验补充

- 全套 `npm run check` 最近一次通过：190 pytest +43 subtests、TS检查、存储/传输/分享检查及构建；之前两处fixture契约漂移已修并保留旧日志。主桥真实WCV/Playwright回归76项通过。视图回归发现测试把“已开始关闭”误当“已完成原生销毁”，已改等待canonical标签实际消失，复验中。
- 原真实run `381fe190c404442894f72b5ddb925524` 保留错误西安记录，重发同一原句后恢复重庆起点/搜索中心、实际walking路线，v2保存为draft_ready成功。之后同run修改预算250，v3正确保留地点与步行；累计12881 tokens仍能继续，基线8338，本轮cap仍12000，没有清计数或提高额度。
- 同run日期修改为2026-09-10/14:00已生效、v4形成，但天气和已过期候选身份依赖尚未真正刷新，正在修该真实发现；不把刷新flags存在当闭环。
- 原准备任务已保存草案，并进入明确的Preparation Goal；重启后的旧tab ID导致只读extract失败，恢复也复制旧ID，正在按原Goal重绑只读观察。已发送/UNKNOWN写命令不重放。
- 原始主库16表逐行摘要、49Cookie、桌面身份/回执及用户融合文档哈希再次核验保持完整，证据在 `eval/plango-live-ui/`。当前新增测试均使用隔离profile/数据库。

- 日期刷新修后已在同一真实run通过：v5仍2026-09-10/14:00/250元，真实高德返回对应日期小雨28℃预报；Discovery实际尝试20个缓存身份、14个成功刷新且发起天气查询。缺失/失败仍unknown，所选当前门店身份已恢复，can_prepare=true。原v4失败保留；新地点输入中的“这个新起点”覆盖前面明确地址问题正在做最后纠正。
- 原准备Goal在桌面重启后已恢复到当前标签，原tab_closed回执保留；之后正确识别18:30与计划18:31差异。现已实现严格同form唯一字段差异的待审批type与原Goal继续入口，实际UI复验中，不人工改fixture凑结果。
- 原拒绝路线可达Reflection的来源问题已修：拒绝/无确认动作不触发模型反思；模型不能写个人偏好。旧偏好保留source/explicit并在界面区分历史记录。当前3专业模型只接当前规范字段，累积goal旧金额不参与生成，原历史照常保留。
- 测试部署脚本新增隔离Compose/URL归属预检，拒绝默认连接主用户服务，避免未来受控测试影响记忆或提醒。无测试URL的预检退出已验证；本轮真实验证一直使用独立plango-e2e与SQLite profile。


## 本轮收尾结果

R0/P0/P1已完成的迁移和执行边界保持。P2/P3本轮的规范、草案/准备、来源刷新、预算恢复、记忆与事件闭环已实现，并完成上述声明范围内的真实用户流程验证；下列未覆盖能力不计为通过。

- 原run `381fe190c404442894f72b5ddb925524` 从错误西安记录恢复，依次完成真实重庆步行草案、保存、预算250、明日日期/天气/身份更新、切换解放碑新起点和搜索中心，v6保存成功。实际地图与路线面板使用计划起点106.575545,29.55772和walking，真实请求/结果为约119米、2分钟；全局原定位不被改写。反馈保存成功且偏好列表仍空，没有由点赞推断偏好。
- 原准备run `d51586748f934797a6c8b202ccd8df5b` 保留全部失败与旧Goal。经过真实UI继续/审批，原生时间18:30→18:31，新快照验收ready_to_review；随后人数改4产生新v4和新审批，真实type使人数3→4，再次ready_to_review。共17条命令（12 extract、2 snapshot、3 type，其中localhost写入按策略被拒，2次允许域名下受控页面type实际执行），无click、提交计数0。此为本地受控原生表单，测试专属host映射不修改生产allowlist，不能冒称真实商家预约。
- 实际外部重庆政府网页读取、真实网页截图的文件选择器上传、浏览器导航/标签/缩放/停止/历史恢复均已验证；分享、反馈、明确偏好、提醒、技能持久化、API断线重连亦有独立真实UI证据。
- 最新完整检查209 pytest+43 subtests、TS/存储/传输/分享/构建通过；其后计划起点/步行地图变更又通过真实Chromium+AMap fixture回归、真实高德UI请求和TS/投影检查。主桥76、原生视图40项通过。
- 主PlanGo服务切换前确认活跃租约/待用户命令/待浏览器命令均0；API/worker已切换新版、PG/Redis健康。切换后16表原始行再次全部保留。主桌面启动与最终本地提交在收尾命令中核验。

### 明确未覆盖与选择

- 本轮没有进行完整质量指标测评、角色收益对比或DOM/Vision大批量sweep，遵循用户最新范围。
- 没有执行真实商家预约提交、下单、支付或外部消息发送。具体商家/OAuth/验证码/闭合Shadow和交易回执适配仍逐站验收；不确定提交保留UNKNOWN及人工核对。微信/飞书产品渠道仍未接入。
- UI没有新增独立单站锁定入口；后端对已有locked状态的保留由真实编译/验证辅助回归覆盖，不把它计作已完成的真实UI点击验收。
- P3采用已有Pydantic、Zod、共享TS协议与持久业务事件/用量记录；没有新增全量OpenAPI类型生成、OTel collector/exporter部署或另一套Agent/记忆框架。这些是明确未选用的可选工程增强，不宣称已经接通。

最终证据入口：[真实界面流程](../eval/plango-live-ui/README.md)、[真实外部网页/图片](../eval/plango-live-browser/README.md)、[原任务准备恢复](../eval/plango-live-preparation/README.md)、[Agent架构与选型](Agent架构与选型.md)。

主桌面最终已通过只读CDP验收：PlanGo标题/品牌、服务已连接、任务输入均就绪；重复start复用PID，没有重复桌面。启动后49Cookie、身份及回执仍与迁移备份一致。独立plango-e2e服务和所有测试桌面/fixture已停止，测试卷、profile与私有SQL备份保留；主plango服务保持运行。

本轮P2/P3与现代UI实现已本地提交：`d6086c1`。此前R0 `4a5bf11` / CI `4451aa4` / P0 `f845e9c` / P1 `15bb44b` 保留分阶段历史；未push或创建PR。当前唯一刻意未跟踪文件仍为用户原文 `docs/YOYU_Planora_融合方案.md`。

### 新会话交接（2026-09-09 08:10）

新增 `docs/CODEX_HANDOFF.md`，集中记录当前代码/服务/数据位置、架构契约、真实与受控证据边界、未覆盖能力、后续建议及验证命令；重写接手提示词，AGENTS 与旧交接均指向新入口。同步纠正架构文档中“目录尚待迁移”的过时状态，历史证据正文保留。

本次只读核实主 API/PG/Redis 健康、worker 运行；8 个主库任务中 6 SUCCEEDED、1 REQUIREMENTS_READY、1 PARTIAL_FAILED，有效租约或 pending_command 为 0，未回执浏览器命令为 0。通过 lifecycle 归属检查和进程复核未发现存活的主 Electron 桌面，与上一轮收尾时“桌面运行”的历史状态区分；没有因此重启服务或新增任务。独立 plango-e2e 服务仍全部停止，原数据与备份保留。本次仅更新交接文档，不重跑业务验收。

交接文档的本地链接、所列路径、代码围栏、diff 格式及配置密钥值泄漏检查通过；用户融合原文哈希不变。独立只读复核确认命令/API/预算与源码相符，真实、受控及尚未覆盖的证据边界无实质矛盾。

## N1→N4 接续实施（2026-09-09，进行中）

从 `ad686e7` 接手，未重复更名/迁移或创建环境。初查仅用户融合原文未跟踪；主 plango API/PG/Redis 健康，8 条任务、有效租约/待命令/未回执浏览器命令均为 0，未发现存活主桌面。以下均为本仓库独立资源，完整质量测评继续暂停。

- 首站有界调查：大众点评真实顺风123(观音桥大融城店)链接跳到 `account.dianping.com/pclogin`；美团重庆旧入口跳到企业官网，移动美食列表在有界等待后仍未加载门店。保留大众点评为候选，尚未称用户已指定或真实菜单已读取。
- 真实扫码页暴露二维码登录检测缺口：没有密码/验证码输入框，模型错误进入读图分支后 PARTIAL_FAILED。已在可信 DOM driver 识别实见登录路由，浏览器/网页规划入口在模型抽取前持久暂停，登录页不算读取完成。原 run `f8f8ad2719074b0a9c62cbb08367892f` 保留失败，同任务恢复并重绑新标签后进入 `authentication_required`；累计2583 tokens未清零，恢复未新增模型用量。真实登录后菜单/套餐、表单准备仍待人工账号接管；未产生外部写入。
- N2 结构化卡/API/稀疏规范编辑和单站锁定已落盘，定向检查与真实复验继续。真实 run `de59413d26ff4b1a958d5d607fc4c6a2` 已从初版重庆解放碑步行草案，完成直接取消预算/人数2→3(v2)、锁定富豪酒家(大都会店)(v3)。原任务、累计用量、未知营业/排队保留。真实复验发现自由方案说明误称“无需长时间排队”，正收敛编译及历史投影/分享的事实表达。
- N3 Linux/WSL 试用打包、配置诊断与冷备份/恢复/升级代码实施中；未发布，也未将构建产物称为已完成独立安装验收。
- N4 已用原始日志修复跨轮历史误判循环，以及取消预算后旧 goal 仍触发预算角色。新增6个带范围回归场景与只读统计；34条归档断言通过，不能当作本次重跑全部真实场景或已量化提速。见[真实用户回归](真实用户回归.md)。

### WSL 意外重启恢复

用户报告应用窗口卡住并重启 WSL。重启后重新查进程/服务归属，没有使用旧PID/CDP操作。主服务自动恢复健康，主库状态与计数不变；两个独立 SQLite 任务仍为 REQUIREMENTS_READY、无pending_command/有效lease，profile与身份文件保留。之前完整 `npm run check` 日志显示213 pytest+43 subtests、TS及构建通过；此后代码仍有定向修订，最终检查待收敛。没有证据把窗口卡住归因于某项代码；后续实际图形窗口逐个启动。用户原文SHA256仍为 `4e63df2f9bf469b142ce6fc03b321f04da8337bf6d0a9ad817433197dca3a70d`。

### 接续收尾验证

- N2同一真实run最终v5：起点民族路188号、搜索中心洪崖洞民俗风貌区、3公里、2026-09-11 15:00、3人、驾车、预算null；实际查询对应日期多云预报。初版→预算/人数→锁定→WSL恢复/解锁→地理/日期/交通全部由UI完成，保存draft_ready。累计14514tokens/164tools；末轮baseline11948/122，增量2566/42，cap保持12000/48/300。再次重启仅恢复显示，不新增模型调用。
- 修正实际发现的自由理由假事实：原v2仍保留“无需长时间排队”失败文本；编译/刷新和历史UI/分享均从结构化人数、估算成本与未知项生成摘要，旧模型draft/rationale仅留审计。真实重开截图已确认当前摘要不提升旧文本。
- N4补修共享消息恢复：原请求历史丢失消息ID后被add_messages重复添加，真实N2形成1→3→7→15→31。现在每轮只追加当前输入，ID绑定run/turn；checkpoint已完成而投影未提交时不重跑节点。原31条历史不删除。离线覆盖同文独立轮、缺checkpoint和两处崩溃边界；真实N1原任务新一轮只从3→4条，未翻倍。此缺陷与WSL窗口卡住的因果未证实。
- N1原run在WSL重启后按原profile恢复；原句中的“不下单”已正确路由menu_read，当前仍停authentication_required。旧2583tokens和旧失败保留，无新模型用量或业务写入；用户尚未完成扫码，真实门店/菜单/套餐/表单仍未验收。
- N3独立安装、包内Electron非开发启动、重复start、v1→v2→v3同安装升级和冷备恢复通过。17表行指纹、72个profile文件、受控Cookie、原身份/UNKNOWN、审批/checkpoint、累计用量、1条Redis PEL保持；恢复启动拦截主机node/npm/conda/uv仍成功。真实失败（镜像并发导出、700目录权限、Electron参数重排）及修复记录均保留，见trial-install.json。两试用项目已停止，卷和备份保留；不是商家履约验收。
- 最终npm check：220pytest+43subtests、TS/存储/传输/分享/构建通过；真实主桥78项、Ruff/mypy67文件、生命周期、试用维护回归通过；8场景47条归档断言通过（脚本live_tests_run=0）。未恢复完整质量测评。
- 主服务更新前租约/待命令再次为0，SQL私有备份保留后切换本项目API/worker；17表1414条原始行、49Cookie及身份/回执全部保持，API/PG/Redis健康、无待命令。主用户桌面profile未打开。唯一保留可见窗口为独立N1扫码会话，原用户融合文件未动；无push/PR/发布。

接续实现已本地提交 `87f18e9`；最终试用归档 `release/plango-0.1.0-linux-x64.tar.gz` 从该干净源码生成，约108MiB，密钥扫描/作者与固定上游检查通过。最终包又完成独立解包安装和全项布尔诊断（未再次启动服务），证据 `eval/plango-next/final-package.json`；完整图形/升级/恢复结果单独保留，不把两者混为一次全链重测。未push/PR/发布。

## 默认窗口与真实扫码接续（2026-09-09）

用户要求默认UI稍微放大，并主动配合扫码。默认窗口从1500×940调为1800×1120，窗口与最小尺寸均受屏幕工作区限制；当前3840×2160 WSL窗口已原地放大，不影响现有会话。Node类型检查及1920×1200隔离真实桌面回归通过；回归截图改存带时间的output目录，保留旧eval证据。

核实原扫码窗口此前已关闭、独立SQLite后端仍存活；只重开同profile桌面，未并行启动消费者或更换身份。用户明确回复已登录后，真实页面已返回顺风123(观音桥大融城店)门店URL与对应标题。原run f8f8ad2719074b0a9c62cbb08367892f继续读取中；登录成功本身不等于菜单、套餐条件或表单准备已完成，后续结果单独记录。

### 登录后读取与可读性修复

用户先完成扫码，再手动完成网站验证中心要求的安全验证；没有自动处理验证码。原profile与run保持，所有临时窗口在不使用时关闭。新实见验证中心路径已在可信driver和后端共同标记captcha，避免旧桌面漏报后进入模型/视觉；后端保守判断也兼容旧客户端。

原任务完成的**真实只读部分结果**：顺风123(观音桥大融城店)，地址观音桥步行街8号附5号大融城6楼6-010；页面人均62元；50元代金券售价47元、精选双人餐98元、顺风冷面鸡单品优惠19.9元；10道可分开的推荐菜。只保留页面原文的周一至周日/随时退/过期自动退预览标签。未将50元面值当售价，也未将页面划线数字解释成已核验原价；完整菜单与使用细则仍缺，当前页面没有原生预约表单。结果保持PARTIAL_FAILED/needs_evidence、read_only、business_completed=false，动作账本0。

真实失败没有删除：首轮Readability将短页裁成推荐菜片段；恢复后通用抽取仍丢地址/售价，改用精确域名、shop路径与顶部店名一致的首站只读版式解析。未知布局继续原抽取，推荐长串不任意拆菜，所有quote是同实体连续原文；旧processed结果不改。新处理共享现有持久写入，受控SQLite重启能从原command重建结构化内容。真实读取累计12152tokens/5tools，末轮并非0模型（一次BrowserDecision请求3343tokens、额外extract及后续model_token_budget fallback保留）；后续已对唯一App下载链接的明确菜单预览停止做定向回归，不能伪称已经取得0模型真实对照。

界面改进：门店/地址/人均和优惠价格/条件优先，SEO原标题及长原文折叠；推荐菜默认8项、其余可键盘展开，未知单价统一说明并禁用无依据比价，反馈移到结果后。当前/历史菜品分类绑定各自证据，未绑定旧条目称“菜品摘录”；PARTIAL不会显示全量完成。额外复现恢复补投影把旧raw命令追加到新结构化snapshot后，使反序去重错选旧结果；现按观测时间并用当前snapshot消歧，保留原历史顺序/内容而不丢新卡片。

本轮最终验证：完整npm check在恢复修复后263pytest+43subtests、TS与构建通过；补BrowserDecision跨累计上限回归后最终全量pytest264+43subtests通过，Ruff/mypy67文件通过，真实浏览器80项、隔离完整桌面与键盘展开回归通过。9个小场景54条归档断言通过（不执行网络）。预算规则没有修改，真实fallback解释与边界有单独回归。

重新打开原任务只展示保存结果，门店地址及47/98/19.9三价格均在UI核对通过，累计12152tokens不变；截图见eval/plango-merchant-live/live-result-final.png。全部临时窗口和隔离SQLite后端已关闭，profile/任务/已登录Cookie保留。主API/worker更新后17表原始行和主profile49Cookie/身份/回执保持，健康且无租约/待命令；私有SQL备份保留。真实商家表单准备、提交/付款仍未验收。

实现已本地提交a05f037；新版 `release/plango-0.1.0-linux-x64-merchant-ui.tar.gz` 从干净源码生成，保留旧包，新包密钥/作者/固定上游/许可证扫描通过。完整窗口/读取/恢复回归与真实只读结果按前述边界交付；没有自动发布或提交外部业务。


## 新会话产品实施交接（2026-09-09 11:02）

用户在确认AI应用/Agent求职方向后，进一步要求按项目本身的后续计划实施，并要求生成新Codex交接。本轮仅整理交接，未改功能、未重跑模型/测试、未打开窗口或重启服务。将此前CODEX_HANDOFF原文完整归档到 `docs/CODEX_HANDOFF_2026-09-09_门店UI归档.md`，重写当前handoff与接手提示词，消除过时“等待扫码/N2N3待做”优先级。

下一阶段尚未实施：先D1发送失败/断线恢复和D2实际配置/能力入口，再D3套餐适用性、D4资料到行程、D5性能/长时稳定、D6路线费用、D7有界真实表单与独立使用验收。输入清空、POST接受后响应/GET失败、创建/消息去重生命周期的风险来自源码审查，先复现，不声称已发生用户数据丢失。求职/作品集建议为辅助材料，完整质量测评仍暂停，外部写入需具体授权。

只读复核：写交接前HEAD46543d6、最新实现a05f037；主Compose归属本仓库，API/PG/Redis健康，worker运行，8任务（6 SUCCEEDED/1 REQUIREMENTS_READY/1 PARTIAL_FAILED）；有效租约/待命令、未回执浏览器命令、RUNNING/UNKNOWN动作均0。没有主桌面或商家/需求/trial目录活跃进程；plango-e2e停止，trial容器无存留但数据保留。商家原run仍PARTIAL_FAILED/12152tokens，需求run v5 SUCCEEDED/14514tokens，受控准备run v4 SUCCEEDED/117705tokens，均无pending/lease。仅核对状态，没有把历史行连续性测试宣称重新执行。

用户融合原文SHA256仍为 `4e63df2f9bf469b142ce6fc03b321f04da8337bf6d0a9ad817433197dca3a70d`，保持未跟踪。新旧试用包及sidecar存在，未发布。

## D1/D2 首批产品实施（2026-09-09）

从`f8eb546`接手，只读核实分支/归属/8条主任务及无租约、待命令、UNKNOWN、桌面进程；没有重做更名、安装、环境或商家扫码。先复现源码风险再修改，所有故障注入使用专属HTTP/SQLite/PG/profile，不称真实用户已丢数据。

- D1：固定request_id与冻结POST内容指纹贯穿renderer→preload/IPC→HarnessClient→API/runtime→RunRepository。main在POST前保存文字、图片、选店、位置、Skills和服务地址；接受与run/命令/绑定/预算在同一SQL事务落盘。生命周期内同ID只接受一次，同文不同轮仍独立。接受后响应丢失/GET失败、重连及重启都先只读核对原接受；不增加累计预算，不重放浏览器副作用。
- 草稿与单个未决发送留在原profile；图片用Chromium原生IndexedDB附件引用，缺图/存储失败保持原请求。界面明确未送达、送达待核实、已接收待取回，未送达才可返回草稿。确认送达只清对应草稿版本，跨会话迟到回包不抢当前任务。旧服务不支持协议时禁止POST并保留输入。
- 保留额外真实复现的受控失败：迟到replan可能覆盖已接受的继续命令；相同ID内容冲突的409响应丢失后，原先仅核对ID会误认旧内容。现事务围栏保护pending/租约/取消/UNKNOWN，接受消息推进版本；所有接受/查询回执比对持久内容指纹，冲突不清新草稿。不是主用户业务事故。
- D2：设置与连接入口共用安全执行服务摘要，区分当前API配置、桌面文本测试、主动服务诊断及最近任务模型历史；独立worker当前配置未单独核对时明示。key只显示配置布尔值，不显示片段；带凭据URL不回显。DOM/Vision、图片、高德、有限公交、App规则和逐项审批边界明确，快捷示例对齐只读/草案能力。没有静默改Docker环境。
- 验证：完整npm check通过279pytest+43subtests、TS/存储/传输/UI/分享及构建；指纹补修后最终全量280pytest+43subtests、类型/传输/Geo客户端/Ruff/mypy69文件/构建通过。两个独立完整runtime对PG各12并发创建/消息/暂停，均1次首次接受+11重放；完成后8次重试不改原row或重排队，同文新ID独立。0012→0013受控迁移保留原行及UNKNOWN。实际Electron主链故障回归POST=1、lookup=2，取回同run；8MB图片两次独立进程完整恢复、重启0POST。界面已目视检查，所有临时窗口关闭。
- 主服务更新：先核实静止并停止本项目API/worker，私有SQL备份后迁移0013并启动新版本。原16业务表1413行保持，迁移版本表按预期更新；主Cookie文件/身份/回执哈希相同。API/PG/Redis健康、worker运行、未决计数0；主profile未打开。测试e2e仅短暂启动PG，已停止；独立v1–v4库及初始化失败记录保留。

证据：[D1/D2](../eval/plango-delivery-next/README.md)，私有备份/日志`output/delivery-next/`与`output/d1-delivery-ui/`。未调用真实模型/商家或恢复质量sweep；旧`a05f037`试用包保留，本轮源码更新未打包发布。用户融合原文哈希不变、未跟踪，未push/PR/发送外部消息。

本轮继续D3同店优惠适用性和D4显式选店/优惠到原行程；D5–D7仍待按序推进，不将首批局部交付当全项目完成。

## D3–D6 产品链与D7有界调查（2026-09-09）

从D1/D2提交`34eb7d0`继续，保持原商家run/profile和全部历史失败。用户报告三项目同时运行及WSL多次崩溃，随后将swap扩至64GB；重新按当前归属查询资源，测试串行且只保留一个临时图形窗口，不操作兄弟项目。扩swap不是根因证明。

- D3：单个持久页面的3项优惠按人数/日期/预算确定性比较，面值/售价/原价/单人/套餐口径分开。真实2→3人使98元双人餐不适用；券和单品不能补足全部餐费，规则/有效期/叠加/额外收费缺失保持unknown。纯浏览器比较不调用模型；进入行程后的需求编辑会重新规划。
- D4：显式核对网页店名/地址与高德`B00178UE0C`，验证者选47元券（面值50），源command/artifact、条目hash和人工对应证据进入同一TripSpec.selected_offer。新读取不会静默替换选中项；来源过期不续时间、不自动抵扣优惠。原完整菜单失败未覆盖，原第6轮窄读取沿“已处理，继续”恢复，仅增2987tokens/1extract，累计15139/6。
- 真实链暴露的旧问题已在共享入口修复：新“当前页”读取沿旧tab导致失败、起点澄清同句时间/时长被丢弃、“更正为”未识别、高德多结果默认取首项、旧草案澄清绑定混进新问题。失败和旧草案保留；最终起点使用真实高德观音桥地铁站`BV10045271`坐标，经既有接口手动设定，不冒充GPS。
- D5：原任务只改人数时候选21→1、当前证据23→3，新增4715tokens/4tools；保留身份/天气/路线刷新，无严格对照不报告提速比例。180秒实际Electron循环156次、52临时标签全部关闭、14次采样、最大响应探测74ms，无新增业务POST/采集到的崩溃或无响应/监听器增长；不证明长期无泄漏或解释WSL崩溃。
- D6：搜索半径与单段路程上限独立，旧规范兼容；同城公交核对两端citycode，完整bus/walking分段累加，标准单人票价乘人数一次，缺票价null。真实高德观音桥站→解放碑读取31分钟/6.206km/3元每人；首次把嵌套空铁路对象误判成真实铁路的失败保留，真实铁路/出租车/跨城仍未知。UI/分享显示餐饮、活动、去程交通及未估项目，预算不保证全部支出。
- 原任务最终v4：2人、2026-09-11 18:30、120分钟、250元，搜索半径0.5km/单段路程2km/步行；0.255km/4分钟，交通0、餐饮估算120。保存draft_ready，累计36243tokens/28tools。v3与v4均实际重启桌面和SQLite后端，原规范/优惠/用量不变；v4优惠旧来源已过期、三项unknown、完整餐费null。未重新扫码，所有临时窗口/后端关闭，profile保留。
- D7：原店大众点评、高德、携程做有界调查，未找到已验证可操作原生网页预约表单；不是证明不存在。未执行真实预约输入/提交、下单、支付或发送邀约；独立用户使用验收尚未发生。完整质量测评继续暂停。

D3–D6完整npm check最终375pytest+43subtests、TS/存储/正确性/UI/传输/分享/构建通过，Ruff/mypy70文件通过。主API/worker更新后原17业务表1413行及主Cookie/身份/回执文件保持，健康且未决0；私有备份`output/product-final/`。证据[产品接续](../eval/plango-product-next/README.md)，原始快照/失败日志留私有output，不公开回调凭据。用户融合原文未改、未跟踪，未push/PR/发布。

用户随后指出聊天区只剩最后回复、要求换明黄色主题；继续沿本轮修复对话历史投影与持久回复，并更新主题，最终验收与提交见后续记录。

### 聊天历史与黄色主题（同日用户反馈）

用户截图确认聊天只保留了当前最后回复；复现来源是renderer每次从state.messages（多数为human）加当前reason重建。现在新可见回复以ASSISTANT_MESSAGE写入原事件账本，与状态同事务；不增加模型messages、不改checkpoint或旧历史，接收回执序号仍指向原输入事件。重复保存/恢复去重，同文新轮独立。前端从完整连续事件恢复问答，旧GRAPH_INTERRUPTED仅恢复已记录的澄清/草案问题，已有AI消息保留；无法找回的旧答复不编造，未执行的新输入不附上旧reason。

按用户偏好将大面积墨绿换为明黄`#FFD100`、暖白底、深灰文字；导航/聊天/设置/成果/浏览器/分享同步，绿仅保留成功状态。聊天显示“你／PlanGo”，稳定消息key，向上查看历史时刷新不抢滚动位置。

验证：新全量后端378项+43subtests通过，1项旧测试假定ACTION_RESOLVED为末事件而失败；更新为分别核对回执与新回复后，原失败及4项持久回复测试共5项通过。TS/存储/正确性/UI/传输/分享、Ruff/mypy70文件、构建与实际Electron完整桌面回归通过；未为配色重跑模型或180秒循环。真实原任务只读重开，13条用户输入、8条已有回复/澄清可见，v4/36243tokens/28tools不变，实际刷新后向上滚动位置保持。窗口/SQLite后端已关闭，profile原地保留。截图与分阶段检查结果见[产品证据](../eval/plango-product-next/README.md)。

随后更新本项目主API/worker，源码hash与运行容器一致，健康、8原任务、未决0；原17业务表1413行及主Cookie/身份/回执再次保持。新私有备份`output/conversation-final/`，此前D3–D6和D1/D2备份保留。旧a05f037试用包未覆盖新功能，本轮没有重做安装或发布包。


## D7真实订位入口与独立试用交付（2026-09-09接续）

从e8e156b继续，初查主API/PG/Redis健康、worker运行、8原任务、租约/待命令/未回执浏览器命令/UNKNOWN均0，无主桌面；WSL约10GB可用、64GBswap仅少量使用，继续串行/单窗口，未碰其他项目。

- 新有界调查4搜索/5页面读取找到重庆尼依格罗酒店悦廊官网Book A Table → szuo/TableCheck真实链接。检索读取预约页超时；独立可见Electron实际打开成功。官网地区跳转到.cn首页、第一次按钮role同时命中wrapper/真实button而未执行的失败均保留。精确核对真实button后，仅从商家须知进入预约控件页。
- 真实页面显示人数/日期/时间/Find availability，截图及字段保存。2人/9月9日为页面默认，不是用户确认；须知含2025旧优惠限制，未提升为当前优惠。重开同profile会回须知页，重新查看后控件可见；尚未填任何字段、查询空位、输入个人信息或提交预约。无新业务run/模型调用，原顺风123任务未变。
- D7入口找到但自动准备仍未完成：该页面没有native form，自定义按钮不满足当前Preparation完整同表单后验，szuo也不在自动写入站点列表。没有伪造form或放宽写权限。已请求用户明确是否以悦廊2人/9月11日15:00做仅准备/查询空位验证；尚未收到确认，后续以实际回复为准再有界适配。
- 独立试用单列3个本人操作任务、记录字段和未覆盖边界，尚无试用者实际结果或用户采用数据。打包脚本只将这份文档加入已有明确文件清单，提交aa3f0cb；trial受控回归、构建及新包内容/密钥排除/作者与固定上游检查通过。新包d7-ready保留product-next及更旧归档，未发布、未重做安装/升级/冷恢复全链、未发邀请。
- 真实浏览期间仅做两次被动资源采样，加载站点和进程数不同，不能据增长判泄漏或解释WSL崩溃。没有运行长时压力/质量sweep。临时Electron/SQLite已关闭，独立profile保留在output/d7-next/tealounge-brha972n/，现场日志在output/playwright/d7-next/。

证据：[D7接续](../eval/plango-d7-next/README.md)、[当前包](../eval/plango-d7-next/package.json)、[真实订位控件](../eval/plango-d7-next/real-booking-controls.png)。主服务本轮未重启，数据/任务/profile保留；用户融合原文未改未跟踪，未push/PR/外部业务提交。

## D7受保护参数预览与无业务写入接续（2026-09-09）

用户已明确授权悦廊2人、2026-09-11 15:00的准备验证，前提是不提交预约、不影响真实业务。本轮先核对实际网站调用，未把“没点最终提交”当成无副作用证明。

- 官方Booking文档存在提前锁位；现场客户端landing自动请求cart/init，Find availability进入创建/更新cart，日历使用私有booking/calendar。不能证明这些私有接口不占位或续期，故在请求发送前阻断全部API/购物车/查询/checkout及未知流量，没有点击查询按钮或放行接口。早期仅UI调查未采集网络，不能追溯断言网站从未初始化过匿名cart。
- 只读保护在主进程导航前启用，精确商家页面及已核对静态资源GET/HEAD才通过；错误query/跳转不能解除，关闭标签后的无归属延迟请求仍拒绝，其他普通标签读取保持。未扩大click/type站点白名单，不依赖模型自称不提交。
- 使用官方文档支持的pax/start_date/start_time链接预填。可信隔离DOM读取实际控件，后端核对原请求、当前URL/快照/时间/保护标记和显示值；自定义控件仍不是native form。新结果scope=booking_parameters，始终business_completed=false/availability_checked=false，年份仅来自URL。界面以参数卡和保护说明展示，不显示无关的零菜品/套餐未知摘要。
- 实际run 82c0f6fbcb394a8eaba38f3c8d6990ea（独立output/d7-next/tealounge-brha972n）完成2人、Fri Sep 11、3:00 pm核对。第1轮initial预算，2240tokens/3tools、2次真实模型抽取。须知暂停后实际重启，旧标签读取失败保留，通过原只读重绑继续完成；共4条持久命令含1次tab_closed，无未回执/UNKNOWN。保存结果又实际重启，同run/结果/用量保持；没有新任务补成功，原顺风123未改。
- 接续补修：只读请求中的“不查询空位或提交预约”此前被误路由write，已在共享否定解析修复；已核对的参数不再因普通无语义目标规则触发Vision；须知继续明确从当前可见标签读新快照并核对来源，避免直接使用已关闭旧标签。对应受控暂停/重启/同预算和反例测试通过。
- 验证：完整npm check在接续补修前386pytest+43subtests、TS/构建通过；最终定向24项及契约8项、Ruff/mypy71文件、TS/UI/构建通过。真实WCV/IPC/Playwright 88断言含本地端点未收到连接、关闭后请求、其他标签、未扩大点击权限。最初自定义HTTPS协议fixture绕过普通webRequest的失败保留，最终用真实HTTPS请求指向本地受控监听器验证，未向商家造测试请求。

所有窗口/独立后端已关闭，profile/任务及失败日志保留。证据eval/plango-booking-preview/；原始现场output/playwright/d7-next/live-1788943739408/，调查脚本在live-1788942063023/。本轮不宣称查到空位、完整表单ready_to_review或预约成功，独立用户验收与长时稳定性仍未完成；完整质量sweep未启动。

主API/worker更新后源文件hash与本仓库一致，健康且未决0；原17业务表1413行及主Cookie/身份/回执保持。私有SQL和指纹备份output/d7-preview-final/，数据库迁移版本保持0013，主profile未打开。

本地实现提交a308b20；booking-preview新包由该干净源码构建，包含本次网络保护、参数卡及独立试用说明，密钥/profile排除与作者/固定上游保留扫描通过。旧d7-ready/product-next归档保持，未push/PR/发布。包记录eval/plango-booking-preview/package.json。


## 两指标评测协议与开发集草案（2026-09-09）

用户要求研究清晰、高可信的TSR与Groundedness评测方法/评测集，Token仅记录。本轮仅研究与准备文件，未执行模型、浏览器、主/隔离服务或质量批次。

- 对照Anthropic Agent评测、WebArena功能结果验收、FActScore原子事实、Ragas Faithfulness、ALCE引用评价及NIST区间方法，形成docs/质量评测协议.md。TSR使用运行前全部must-pass及禁止副作用条件，不靠产品SUCCEEDED/Verifier自判；完整性纳入TSR，不新增第三个质量指标。
- Groundedness逐条核对原子事实的实体、来源、时效、量纲/推导与冲突，单题支持数/事实数，主报告使用任务宏平均。只有零事实才N/A，含事实断言的拒答仍评分；相同语义事实去重，交付过的错误不被末轮纠正抹去。
- 设计12条公开dev（6家族各2）＋30条新采留出（20可交付/10范围判断）＋3条单列实时哨兵。所有现有门店/路线/pilot都已被开发看过，只能dev。独立标注者从原始材料写gold，不能抄系统加工输出；模型输入、环境/故障编排和gold文件分开。
- 已写eval/quality-v1/tasks.dev.json、gold.dev.json、sources.dev.json、split-plan.json及空标注表。均为AI起草待人审，未伪造留出或金标。源文件及原始子树hash只作溯源；公交请求时间元数据、完整DOM/样式等仍有缺口，明确当前观测注入不等于完整Electron端到端回放。
- 独立复核发现raw价格文本确有邻接120/32数字，不能照抄产品original_price=null作gold，也不能凭数字认定原价；须按输入证据粒度判定或补DOM/截图后人审。恢复case删除预置保存成功/伪接受指纹，改为真实初始化、保存/接受后才注入故障并核对状态。
- 方法复核修正了invalid分母与一致重判、拒答中的事实、test泄露的主体/时间、跨轮重复事实等歧义。scripts/check_quality_design.py仅静态检查12案例结构、环境/金标隔离及3份原始来源hash，输出evaluation_ready=false、model_evaluations_run=0；脚本Ruff通过，未跑产品测试套件。

当前有方法和开发稿，没有正式质量分数。后续先完成人审/环境适配，再在冻结版本上采集封存未见集；既有质量测评暂停保持。主服务/任务/profile/密钥及用户融合原文未改，无push/PR/发布。

## 两指标评测管线实施与首轮dev采集（2026-09-09）

用户随后明确要求“现在开始实施”，授权本轮有界开发评测；原大规模质量/角色收益/DOM-Vision sweep暂停不变。复用本项目Python/模型与已有React投影、持久事件和请求接受链，未新增依赖或另建产品事实库。

- `quality_runner.py`默认仅预检，执行需同范围源码manifest匹配；每例独立SQLite/checkpoint/profile、串行动态回环API。8例冻结原始观测注入、4例显式未保存草案导入，后者2例实际Electron；不是完整商家网页回放或未见盲测。业务日期/原始观测时间适配不冻结SQL租约/执行超时；只允许模型出口及自有API，未接触真实交易。
- 导出器使用真实`projectHarness`及React卡片，涵盖每轮回复、候选与折叠源摘录；桌面故障中间停点保留原快照/UI文字/截图，历史停点没有即时SQL时不伪装。恢复测试真正保存一次，或在真实202接受后丢响应并使lookup失败，再点原UI取回；不伪造接受/完成状态。
- 原始来源包与oracle保留60项must-pass，仅6项能凭独立SQL确定性检查。评分器强制覆盖、来源定位和明确invalid替代关系；零事实N/A、缺标注pending，不从phase推成功。人工工作页不代签，30条留出尚未采集。
- 首轮`dev-20260909T112246Z-eeb85930`采12/12，25次真实模型调用、34039tokens，无缺usage。6项SQL条件5通过1失败：DEV-05取消预算成功但人数仍2；DEV-06距离/日期字段正确，后续额外起点澄清。DEV-10真实POST=1、lookup恢复且轮次/接受记录各增1，但人数仍2；DEV-04/08分析被转入定位澄清。全部保留，不以采集完成或局部恢复当TSR成功。
- 真实judge接线仅DEV-04，1调用4461tokens；证据ID/缺引用不合法，验证器保持待审、不重试。合计38500tokens。其他大包超过现有单轮预算则保留人工复核，未加预算/裁掉事实。完整标注0/12，TSR/Groundedness均null，尚不能作为简历质量百分比。
- 23项离线测试＋15subtests、Ruff、真实React导出自检、driver语法检查通过。修复测试驱动上游SSE残留、浏览器运输等待误报缺回复和安全预填链接被脱敏丢参数；最初离线driver完成后不退出的失败保留，只终止核实归属且窗口/代理已关闭的两个Node，未伪装质量成功。

证据[质量实施](../eval/quality-v1/README.md)、[脱敏摘要](../eval/quality-v1/pilot-2026-09-09.json)，私有run/原始judge响应/hash均保留。测试窗口/API/代理已关闭，DB/profile不删除；主API/PG/Redis只读核实健康，worker运行，未修改/重启主服务或兄弟项目。用户融合原文保持未跟踪；无push/PR/发布/外部消息。

采集器阶段提交`c15217d`与首轮actor输入/代码hash对应；其后补齐`Evidence.expired`内部业务时钟，历史日期离线验证通过，未再跑actor。独立复核修正了评审输入中重复计算导入初态旧回复、遗漏受控路线原始fixture两个问题：原始导出不改，按baseline消息ID排除旧消息，当前卡片完整，fixture须校验导入/manifest/实际文件hash与输出前取得时间。旧评审代码和失败响应保留；最终零调用重新组包12例无材料错误，仍是0/12完整标注。最终回归23项＋15subtests通过。

## 根据dev失败修复需求编辑与资料分析（2026-09-09）

用户要求先实施修复。初查工作树仅保护原文未跟踪；主API/PG/Redis健康、worker运行、8任务及租约/待命令/未回执/UNKNOWN均0。WSL约11GB可用、64GBswap已用约4GB；串行验证，没有触碰其他项目。

- `7e63cc8`：普通“人数”标签未进入总人数提取，随后稳定化无条件用fallback空值覆盖模型字段，原2人遂保留。补共享解析及否定/空格角色边界，非法数量和未定状态仍澄清；即使模型给不同默认值，明确3人也稳定落库。修复日期被地理正则提取为搜索地区，并保留同句“日期保持但起点修改”和真实地名正例。上一批所谓“起点澄清”应准确表述为search_location澄清，原起点坐标始终保留。
- `aeb798a`：明确资料核算不再进入定位规划。复用现有持久页面与reasoning结果，增加带连续原文引用的结构化抽取及确定性条件/金额核验；单位、来源唯一性、否定、人数/日期/时段和缺费条件不满足时保留未知。没有用模型rationale/finish作事实、另建事实状态或加交易权限。
- 首轮原5例真实复验：DEV-05为3人且两预算清空；DEV-06日期9/12、radius0.5、路程null、无日期geocode；DEV-10原请求POST=1、真实202丢响应/首次lookup失败后取回、轮次/接受记录各增1、人数3，canonical门店/优惠/起点保持。两条分析仍PARTIAL_FAILED，保留在`output/quality-v1/dev-20260909T121954Z-4c5e8be0/`，21调用27576tokens。
- `0eba55c`：独立复现有效短引文会被整字段标签要求拒绝。改为核对短引文在原文中的唯一位置和数字span，完整原文仍校验单位/否定并展开保存；补实际后端/回执/browser_decide集成测试。旧模型提案未记录，不能倒推其具体原文；后续仅在私有dev日志记录SourceAnalysis提案，不作产品事实或评审来源。
- 仅复验原DEV-04/08，`output/quality-v1/dev-20260909T122835Z-10cdd6a3/`，4调用2594tokens：套餐98元条件核算，明确两位须符合成人范围；路线保留餐费180、2.4km/15分钟和20元预算余量，票价未知、完整总价null。当前scope=source_analysis、business_completed=false，非商家保证/交易。
- 本轮合计25调用30170tokens，旧失败与累计用量保留，未修改task/gold/fixture或运行judge/大sweep。完整npm check通过426pytest＋58subtests及前端/构建；最后补修的相关23项、Ruff和mypy26文件通过。不是正式TSR/Groundedness，金标、事实标注、留出仍待人审/采集。

主服务更新前保存18表1414行指纹及SQL一致性备份，3个Cookie/身份/回执文件hash；再次确认未决0后只更新本项目API/worker。更新后源码hash一致、健康，原17业务表1413行及桌面资料保持，迁移0013未变。证据[修复记录](../eval/plango-quality-fixes/README.md)，私有备份`output/quality-fixes-main-20260909/`。临时窗口/独立API/代理已关闭、DB/profile不删，未操作兄弟项目，保护原文不改不提交；未push/PR/发布或发生真实交易。

## 30例新受控评测与共享提示修复（2026-09-09）

用户明确要求30例新案例，并将原先人工审核改为新开独立子Agent代审；后续强调从跨案例共因修复系统，避免单题规则和过拟合。本轮冻结产品后生成新题，原Actor输出始终保留，未根据有效失败继续调产品或挑最高分。

- 固定产品`de37fecd`及140个文件/安全模型配置；独立上下文作者未读产品、旧dev和输出，生成15组虚构来源、6家族各5例，20交付/10范围判断。20例资料观测注入、10例实际Electron编辑/保存重启/真实202丢响应及取回。独立AI审核金标，修正一项严格时间边界后30例批准；原审查与空人工事件文件保留，人工审核0，不称人工金标或真实网站泛化。
- `b1434b0`实现独立AI签核入口、冻结/尝试登记、真实桌面driver配置及静态源/初态分离。`23c6201`修复导入来源ID与类型契约不一致并在持久化前校验：原8次受影响桌面尝试统一独立裁定无效，不能只作废失败题。`d29b7b1`补回环API直接连接与输入前连通探测；NEW12一次零输入/零模型的连接超时独立判为初始化故障，再作一次替代，不把代理猜测当已证明根因。
- 最终39次尝试中30次有效、9次独立裁定无效；有效54调用/63702tokens，含无效共76调用/97924tokens，无缺usage。不含Codex起草/审核消耗；保留所有失败和累计用量。串行、独立SQLite/profile、受限模型出口，未接触商家或运行20任务/角色收益/DOM-Vision sweep。
- 30题实际输出全部封存；独立AI逐项检查must-pass、禁止条件、操作范围以及所有回复/卡片原子事实。复核发现组包白名单漏掉原已记录的request/202、保存基线和持久事件。`0707335`统一补入10个桌面题已有运输证据，`4215d9f`处理标准观测时间；逐项核对原hash，原输出/来源/gold/状态不变，无Actor重跑，旧包与初审保留。新增时序检查禁止用未来回执或汇总支持早输出。原snapshot内保存请求已有plan/version/command绑定，不把导出遗漏误归产品故障。
- 用户截图的技术异常在独立worktree修复，等原30题输出封存后才合入`b55371d`。共用`userMessages`覆盖聊天/历史/优惠/浏览器/发现/设置/分享/操作记录，后端优惠投影给旧客户端也返回中文；日志只保留类型/字段位置，不输出校验输入和凭据。送达状态、机器409、原请求身份、审批与UNKNOWN不变。`a7b8b53`补真实桌面回归及测试模块导入兼容。不是只替换一个截图文本，也没有为评测某个问法增加业务规则。
- 验证：双端TS、UI/运输/LLM与历史定向检查、构建、隔离API投影1项、Ruff/mypy通过；实际Electron/WCV/IPC回归覆盖旧后端异常显示、原优惠保留、保存重启、一次POST与原请求取回。评测证据/时序/AI聚合相关离线回归12项+29subtests通过，新增observed_at兼容的定向3项通过。旧失败日志保留，未用模型调用代替工程验证。

最终独立AI复核30/30完成，无缺项：**TSR 10.0%（3/30）；Groundedness 54.3%**，按30题宏平均，384条支持事实/541条事实，N/A为0。NEW17/19/30整题通过；NEW18虽取回/字段通过，未达预注册draft_review停点仍判失败。原低分基线保留，不改成高泛化主张。质量结果与明确分母见[30例报告](../eval/quality-v2-30/README.md)，展示修复见[提示证据](../eval/plango-friendly-errors/README.md)。主API/worker已更新、源码hash一致且健康，原8任务、17业务表1413行与3个Cookie/身份/回执文件保持，未决/UNKNOWN为0；备份`output/friendly-errors-main-20260909/`。临时桌面/独立API已关闭，评测DB/profile、原商家任务和历史证据保留。用户融合原文未修改且未跟踪，无push/PR/发布/外部消息/真实交易。

后续优先处理三个共同问题：任务目标与所需资料的路由、明确字段修改与持久状态的绑定、来源/费用范围和交付文字的一致性。复用原TripSpec/稀疏补丁/来源/审批机制，验证新的同义表达、单位变化、否定及“其余不变”反例，不堆题号/门店/单句特例。该30题已揭示，只能作为后续回归，不能冒充新版本未见留出。

## 质量优化与复测的新对话交接（2026-09-09）

用户要求编写可无缝接续的handoff。本次仅重写当前交接和接手提示词、同步AGENTS入口，不修改产品或运行新模型/浏览器/质量批次。交接前HEAD为bed0aac，文档提交以实际Git为准。

只读重新核实：主API/PG/Redis健康、worker运行，8任务（6 SUCCEEDED/1 REQUIREMENTS_READY/1 PARTIAL_FAILED），有效租约或pending/未回执/RUNNING及UNKNOWN均0；主容器目录归属和runtime源码hash匹配。无主桌面/隔离评测driver或API，plango-e2e停止。WSL约12GB可用、64GBswap用约4.5GB；旁支友好提示worktree改动已合入，遗留node_modules符号链接不提交，原资料保留。

清除了旧提示词“D7优先／所有质量测评暂停／暂无分数”等过时引导。当前文件明确三种版本：de37fecd被测基线、b55371d/a7b8b53部署版、a308b20安装包；新版暂无新质量成绩。保留用户反局部过拟合、AI独立审核、模型预算、业务无副作用、WSL负载和数据/密钥约束，写明TSR10.0%与Groundedness54.3%的范围、invalid替代和运输补包的区别。

下一轮需沿共享需求更新/任务路由/事实交付实施修复；旧30题只作回归。离线复算命令已提供，新产品采集入口尚待显式数据集/冻结路径接线，不能覆盖旧freeze/registry或绕过保护，不能把现成离线复算冒充新版模型复测。新泛化评测先冻结、再独立按来源组采样/审gold/运行/审输出。未push/PR/发布，未重启服务；保护原文未改且未跟踪。
