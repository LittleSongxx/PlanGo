# PlanGo 新 Codex 接手提示词

更新：2026-09-09。复制下面全文至新对话。R0 已实施并验证主要数据链，最新阶段与遗留工作必须先读 docs/实施进度.md；不要重复迁移已完成数据。

```text
请接手本项目，直接核实状态并实施，不要只输出分析或询问“是否开始”。

当前目录：/home/song/code/Agent/multi-agent/PlanGo
目标目录：/home/song/code/Agent/multi-agent/PlanGo
预期分支：feat/planora-browser-harness；以实际 Git 状态为准。如果目录已迁移，定位同一仓库继续，不创建空项目覆盖。

先阅读 AGENTS.md、docs/CODEX_实施交接.md、docs/架构决策.md，再按需阅读 docs/模块替换与Agent工作流审计.md 和 docs/闭环与设计成熟度评审.md。核实工作区修改、运行会话、服务与任务，保留已有成果，避免重复施工。历史报告是证据与定位入口，不代表问题已经修复。

最新要求：将整个项目全面更名为 PlanGo。产品名用 PlanGo，技术标识用 plango，自有环境变量用 PLANGO_。更名范围包括 UI/窗口/应用标识、前后端源码及导入、内部 Harness 包、包元数据和锁文件、Docker 项目/容器/镜像/自有存储、conda 环境、配置、启停脚本、测试、文档、资源和项目目录。旧品牌还包括“小悠”、xiaonian 桥接/配置/浏览器分区及 xy_* 状态键；作者署名不作品牌替换。不得只替换 UI 文案。

先完成交接文档 R0，再继续 P0→P3：
- R0：核实基线，备份数据，完成全面更名并验证升级和全新安装。主包迁往 backend/plango；内部 Harness 使用独立的 PlanGo 命名，保留真实上游版权、许可证与固定来源。创建独立 conda plango，本机与 Docker 均切换；不重命名、删除或修改原 planora 环境。Compose 项目用 plango，镜像用 plango-harness:local。迁移数据库、数据卷、Redis 任务、浏览器登录态/设置、回执、审批和幂等记录；不能启动一个空数据库冒充迁移完成。先核实实际存储，不新造不存在的存储层。持久状态中的身份变化不得放宽审批或重放 UNKNOWN 提交。更新路径后在安全收尾阶段将仓库目录迁往 PlanGo，避免破坏当前会话和已有服务归属检查。
- P0：修复地图不可信 HTML、证据三态、规划/图片入口到执行的目标传递、普通点击提前终止、错误完成判定及 UI 状态/定位来源失真。
- P1：实施 WebContentsView 与经验证的 Playwright/CDP，先保证同一真实可见会话和稳定连续 DOM 操作，再加入按需 Screenshot/Vision fallback。
- P2：统一需求补丁、执行目标、结果验收、地理上下文和依赖刷新；整理 LangGraph 的明确工作流，区分协调器、专业 Agent 与验证/存储等服务。
- P3：按实际收益完善类型协议、事件重连与重放、模型错误分类、记忆读写闭环、可观测性和结果落库/任务恢复一致性。

固定方向：保留 Electron + React，保留丰富产品功能、真实可见浏览器、人工接管与登录态。UI、模型和执行器操作同一浏览器会话。DOM-first + 按需 Vision 共用授权、页面版本、幂等、回执与结果验证，不能绕过拒绝授权、验证码/登录接管或 UNKNOWN 提交。任意 JS/CDP 权限不能暴露给模型或不可信 renderer。不要假设 Electron/Playwright 的示例能覆盖所有嵌入页面，先做真实兼容性验证。

更名时特别检查已持久化的包路径与 LangGraph checkpoint、Redis 待确认消息、桌面身份和未完成回执。数据库改名使用新增迁移，保留已应用的 Alembic revision ID；不能通过扩大反序列化白名单解决历史包名兼容。详细入口见交接文档 R0。

项目必须完全独立。只修改本仓库及其专属服务；允许修改当前 vendor/planora 固定副本并迁移其运行时命名，禁止修改或运行依赖同级 ../Planora 仓库、配置、服务和测评。不盲目照搬它的实现或质量结论。

保留默认重庆和已有 OpenAI-compatible 模型/高德配置。读取本项目 .env 即可，更名配置键时保留值与权限，不输出或提交密钥；保留 OPENAI_*、AMAP_* 标准变量。保留用户未跟踪的 docs/YOYU_Planora_融合方案.md 原文，不覆盖、删除或顺手提交。历史证据和第三方署名不做机械替换；必要兼容项要记录原因和退出条件。

实施中独立调研、查证官方资料，优先复用已有代码、依赖和成熟方法，选择能解决问题的最简单方案。交接建议不是安装清单。追踪真实调用链后修根因，将机械更名和行为改变分开验证。适时并行委派互不冲突任务，全局更名统一协调。

按改动运行必要回归、真实 Chromium/Electron 和 Docker 集成检查，验证新 conda 解释器、容器健康、数据连续性及 start.sh/stop.sh 的重复执行和归属隔离。区分单元测试、真实模型加合成页面、真实浏览器加受控页面、真实外部网站任务；保留失败分母，不以 mock 成功或旧报告冒充新验收。

已授权适量真实模型和高德只读调用；真实下单、支付、发送消息等业务写入仍需具体授权。重启前检查进行中任务，切换旧/新 worker 时防止重复消费，保留可恢复的数据备份，不影响其他项目。

维护 docs/实施进度.md，持续记录实现、迁移、验证、遗留问题与下一步；同步 README、架构、部署与交接文档，最终命令全部使用新环境和包路径。验证后可分阶段本地提交，不自动 push 或创建 PR。完成一个阶段后继续后续核心实施，不把局部完成当成全项目完成。

现在先核对状态及实施进度，复验已完成 R0 数据连续性，随后直接继续尚未完成阶段。
```
