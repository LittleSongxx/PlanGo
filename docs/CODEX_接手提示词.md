# PlanGo 新 Codex 接手提示词

更新：2026-09-09。当前完整交接见 [CODEX_HANDOFF.md](CODEX_HANDOFF.md)。复制下方文本到新对话即可；仓库已经迁入 PlanGo，R0→P3 本轮核心实施已完成，不要重复旧迁移。

```text
请接手 /home/song/code/Agent/multi-agent/PlanGo 项目，直接核实并推进后续工作，不要只输出分析或询问“是否开始”。

先阅读 AGENTS.md、docs/CODEX_HANDOFF.md、docs/架构决策.md、docs/实施进度.md 末尾“本轮收尾结果”和 docs/Agent架构与选型.md。新 handoff 是当前实施入口；docs/CODEX_实施交接.md 及进度中的旧“正在修/待复验”保留历史基线，不能当作最新待办。

当前分支预计为 feat/planora-browser-harness；最近实现提交 d6086c1，随后有交付/交接文档提交，以实际 Git 为准。先检查已有修改、服务和进程归属、有效租约与待命令。写 handoff 时主 plango Docker 服务健康、没有活跃租约或待浏览器命令；未发现存活主桌面进程，旧 PID/CDP 文件仍在；隔离 plango-e2e 服务已停止但数据保留。重新核实后按需启动本项目桌面，不依据历史 PID 操作。

已经完成全面更名、目录迁移、独立 conda plango、数据库/Redis/桌面无损迁移，以及 P0/P1 正确性与可见浏览器升级、P2/P3 规范/草案准备/恢复/记忆/事件闭环和现代 UI。不要重新创建环境、迁移已有目录或重建空库。

接着按 handoff 第 7 节推进：
1. 选择一个真实可达的首站，补齐真实门店/菜单或套餐条件读取、登录人工接管、表单准备和原任务恢复。
2. 增加直接编辑起点、搜索范围、日期、人数、预算、交通模式的结构化需求卡与单站锁定，复用当前规范补丁/版本/依赖刷新机制。
3. 完成可独立安装的试用版本、非开发启动、配置诊断和已有数据升级/备份恢复。
4. 用实际日志减少重复查询和无效模型循环，维护小型真实用户回归集。
首站还未指定，不虚构已确认平台；依据现有入口和可达性做有界选择。完整质量指标测评仍暂停。MCP、更多 Agent、OpenAPI 生成或 OTel 部署只在有明确需要时引入。

保持 PlanGo / plango / PLANGO_ 命名、默认重庆和本项目已有模型/高德配置；不输出或提交密钥。只修改本仓库及专属资源，禁止修改、依赖或重启兄弟 ../Planora 仓库、服务、测评和原 conda planora 环境。保留用户未跟踪的 docs/YOYU_Planora_融合方案.md 原文，不修改、删除或提交。保留作者署名、许可证、上游来源和历史证据。

保留 Electron + React、真实可见浏览器、WebContentsView + 已验证的 Playwright/CDP、DOM-first 与按需只读 Vision；UI/模型/执行器共用会话及 Harness 授权/回执边界。集中式工作流、外层 Plan-and-Execute、浏览器有界 ReAct；single/multi 是视角策略，不扩成无需求的自治多 Agent 团队。

完整保留数据库、浏览器登录态、任务、checkpoint、审批、幂等与 UNKNOWN 回执。不清库、不重置 profile，不以重新授权或重放不确定写入制造成功。只读、图片识别、草案和准备待核对分别报告范围，不能都当成业务完成。

已有证据区分清楚：真实模型/高德规划与外部政府网页读取已验证；原任务准备链虽经真实 UI 执行，商家表单仍是本地受控样本，提交计数 0。真实商家预约、下单、支付和外部消息尚未验收；适量真实模型/高德只读调用已有授权，外部业务写入仍需具体授权。

按改动完成必要验证，真实测试使用归属明确的独立 profile/数据库，保留旧失败和累计用量，不通过提高预算掩盖错误。维护 docs/实施进度.md，同步当前 handoff、架构和使用文档；可分阶段本地提交，不自动 push、创建 PR 或发布。不要把局部完成当全项目完成。
```
