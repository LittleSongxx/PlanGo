# PlanGo 新 Codex 接手提示词

更新：2026-09-09。当前完整交接见 [CODEX_HANDOFF.md](CODEX_HANDOFF.md)。复制下方文本到新对话即可；仓库已经迁入 PlanGo，R0→P3与N2/N3接续工程已完成；真实商家登录后的读取/准备仍未完成，不要重复迁移或需求卡/安装器施工。

```text
请接手 /home/song/code/Agent/multi-agent/PlanGo 项目，直接核实并推进后续工作，不要只输出分析或询问“是否开始”。

先阅读 AGENTS.md、docs/CODEX_HANDOFF.md、docs/架构决策.md、docs/实施进度.md 末尾“N1→N4 接续实施/接续收尾验证”和 docs/Agent架构与选型.md。新 handoff 是当前实施入口；docs/CODEX_实施交接.md 及进度中的旧“正在修/待复验”保留历史基线，不能当作最新待办。

当前分支预计为 feat/planora-browser-harness，实际提交以Git为准。先检查已有修改、主/隔离服务和进程归属、有效租约与待命令。用户曾因WSL窗口卡住重启WSL；不要按历史PID/CDP文件操作。最新handoff第0节记当前状态，第3节保留上一轮08:10历史快照。

全面更名/目录迁移/独立conda plango和R0→P3保持；接续已经增加结构化需求卡、单站锁定/解锁、真实日期/地理依赖刷新、Linux独立试用安装/升级/冷备恢复和小型回归集。已修正扫码登录漏检、“不下单”意图、自由理由冒充商家事实，以及历史goal/跨轮循环/消息倍增；原失败及31条重复历史保留。不要重新创建环境、迁移目录、重建空库或重复已完成入口。

接着优先推进N1尚缺的真实商家链：
1. 当前保留大众点评为候选首站，尚非用户指定平台。真实顺风123观音桥大融城店链接跳转扫码页；美团旧入口到企业官网、移动列表未加载，不能把公开索引店名当当前商家验证。
2. 原run f8f8ad2719074b0a9c62cbb08367892f 在 output/merchant-next/session-C0ovDv/ 独立SQLite/profile，累计2583tokens，停authentication_required；若可见窗口仍在，请让用户自行登录，随后在原run读取真实门店/菜单/套餐来源。无账号时明确缺口，不反复探测其他受保护接口或复制Cookie。
3. 如有真实可操作表单，按现有逐项审批准备与原Goal恢复验证；没有表单就报告只读范围，不用本地fixture补齐，更不能提交交易。
4. N2真实任务 de59413d26ff4b1a958d5d607fc4c6a2 已在同一任务5版与WSL恢复后保存draft_ready；N3源/恢复安装均停止保留卷。后续依据实际日志继续减掉必要场景中的冗余，不恢复完整质量测评。

主plango API/worker已更新接续源码；17表1414原始行与49Cookie/身份/回执保持，无待命令；主用户profile桌面未打开。独立扫码窗口不是主用户桌面，启动/停止前重新核实归属。试用包、验证日志和端口以当前handoff及eval/plango-next为准；本地包未发布。MCP、更多Agent、OpenAPI生成或OTel仅在明确需要时引入。

保持 PlanGo / plango / PLANGO_ 命名、默认重庆和本项目已有模型/高德配置；不输出或提交密钥。只修改本仓库及专属资源，禁止修改、依赖或重启兄弟 ../Planora 仓库、服务、测评和原 conda planora 环境。保留用户未跟踪的 docs/YOYU_Planora_融合方案.md 原文，不修改、删除或提交。保留作者署名、许可证、上游来源和历史证据。

保留 Electron + React、真实可见浏览器、WebContentsView + 已验证的 Playwright/CDP、DOM-first 与按需只读 Vision；UI/模型/执行器共用会话及 Harness 授权/回执边界。集中式工作流、外层 Plan-and-Execute、浏览器有界 ReAct；single/multi 是视角策略，不扩成无需求的自治多 Agent 团队。

完整保留数据库、浏览器登录态、任务、checkpoint、审批、幂等与 UNKNOWN 回执。不清库、不重置 profile，不以重新授权或重放不确定写入制造成功。只读、图片识别、草案和准备待核对分别报告范围，不能都当成业务完成。

已有证据区分清楚：真实模型/高德规划与外部政府网页读取已验证；原任务准备链虽经真实 UI 执行，商家表单仍是本地受控样本，提交计数 0。真实商家预约、下单、支付和外部消息尚未验收；适量真实模型/高德只读调用已有授权，外部业务写入仍需具体授权。

按改动完成必要验证，真实测试使用归属明确的独立 profile/数据库，保留旧失败和累计用量，不通过提高预算掩盖错误。维护 docs/实施进度.md，同步当前 handoff、架构和使用文档；可分阶段本地提交，不自动 push、创建 PR 或发布。不要把局部完成当全项目完成。
```
