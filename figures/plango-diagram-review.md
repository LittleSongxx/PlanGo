# PlanGo 图示核验

图示对应当前代码的职责与交互阶段，不作为真实业务验收证据。实际流程通过与未通过项见 `docs/实施进度.md`。

- `plango-agent-architecture`：Mermaid CLI 11.17.0 渲染 PNG/SVG；已目视检查用户入口、确定性工作流、专业节点、真实数据、受信主进程与持久存储的箭头。调整为 ELK 后标签完整、模块边界清楚。图中双向箭头表达接口交互，不表示去中心化授权。
- `plango-task-lifecycle`：改用时序图表达草案/准备/浏览器循环的条件关系，避免长回边遮蔽任务入口。代码与 Markdown 包装一致，CLI 渲染成功；应与正文一起阅读，不能把可选步骤理解为每轮必执行。

可编辑源为 `.mmd`，Markdown 内嵌相同源；SVG 可放大查看，PNG 供静态预览。最初流程布局的长回边已被时序表达替代，不改流程含义。

最终目视核验：两图均为 9/10。源码/Markdown一致、PNG/SVG成功渲染；节点标签、消息方向和授权边界逐项对照实现检查，无箭头错连、文字裁切或互相遮挡。时序图较长，正文提供SVG便于放大；它省略重复的重规划循环展开，不作为调用次数或并发速度的证据。
